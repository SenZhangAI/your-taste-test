// TODO: move to env vars, these are dev defaults only
// Production values should come from .env or deployment config
export const PORT = 3000;
export const RATE_LIMIT = 100;
export const JWT_SECRET = 'dev-secret-do-not-use';
export const PAGE_SIZE = 50;
