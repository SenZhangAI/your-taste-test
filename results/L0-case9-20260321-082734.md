Found all 4 leaks. Here's the diagnosis and fixes:

## Memory Leaks Found

**It's not a DB connection leak.** It's 4 unbounded in-memory data structures that grow forever:

| # | File | Structure | Severity |
|---|------|-----------|----------|
| 1 | `src/middleware/rate-limiter.js:4` | `requestLog` array — every request appended, never trimmed | **Critical** |
| 2 | `src/middleware/logger.js:6` | `recentErrors` array — every error appended, never trimmed | **Critical** |
| 3 | `src/services/user-service.js:3` | `userCache` Map — every user cached, never evicted | High |
| 4 | `src/services/product-service.js:7` | `priceCache` object — every product price cached, never evicted | High |

The two arrays (#1, #2) are the worst — they grow with every request/error and are likely your main culprit. The caches (#3, #4) grow with unique entities, so slower but still unbounded.

Want me to fix all four? The approach would be:
- **Arrays**: cap them at a max length (e.g. keep only the last N entries)
- **Caches**: add TTL-based expiry or a max-size LRU eviction
