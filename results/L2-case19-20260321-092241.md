All changes look correct. Here's the summary:

**Fixed bugs:**
- `softDeleteOrder` now sets `deleted_at` instead of `status = 'deleted'` — this also fixes the **latent bug** where `getUserOrders` in `user-service.js:36` filters by `whereNull('deleted_at')` but that column was never being set

**Standardized soft-delete on `deleted_at` across all three tables:**

| File | Change |
|------|--------|
| `src/services/order-service.js` | `softDeleteOrder` sets `deleted_at`; all filters use `whereNull('deleted_at')` instead of `status !== 'deleted'` |
| `src/routes/orders.js` | GET/DELETE checks use `order.deleted_at` instead of `order.status === 'deleted'`; stats query uses `whereNull` |
| `src/services/product-service.js` | `listProducts` uses `whereNull('deleted_at')` instead of `is_active`; added `softDeleteProduct` |
| `src/routes/products.js` | GET check uses `product.deleted_at` instead of `!product.is_active` |
| `src/db.js` | Added `deleted_at` column to products schema |
| `migrations/004-add-deleted-at-to-products.js` | New migration adds `deleted_at` to products, migrates `is_active=false` rows |
| `docs/API.md` | Updated to accurately describe the unified pattern |

**Note:** `is_active` column is kept in the schema for backward compatibility — the migration handles data migration from `is_active=false` to `deleted_at`. You can drop `is_active` in a follow-up migration once all consumers are updated.
