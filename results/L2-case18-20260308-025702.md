Done. Changes:

- **`src/services/order-service.js`**: `listOrders` now joins `users` on `orders.user_id` and selects `users.name as user_name`
- **`src/routes/orders.js`**: Added `user: o.user_name` to the GET `/api/orders` response

Each order in the list now includes a `user` field with the name of the person who placed it.
