Done. Here's the summary of changes:

**`src/services/order-service.js`:**
- `listOrders` — both the query and count now use `.whereNull('deleted_at')`
- `searchOrders` — same filter swap
- `getOrder` — added `.whereNull('deleted_at')` so soft-delete filtering is centralized in the service (was missing before — callers had to check `status === 'deleted'` themselves)
- `softDeleteOrder` — sets `deleted_at` timestamp instead of `status: 'deleted'`

**`src/routes/orders.js`:**
- GET `/:id` and DELETE `/:id` — removed redundant `order.status === 'deleted'` checks (now handled by `getOrder`)
- GET `/stats` — direct query updated to `.whereNull('deleted_at')`

One thing to flag: `seed.js:45` still seeds some orders with `status: 'deleted'`. You'll want to update that to set `deleted_at` instead so seeded data matches the new convention. Also `validators.js:26` has a comment referencing the old pattern — minor but worth cleaning up when convenient.
