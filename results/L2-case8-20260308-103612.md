Summary of all fixes:

| Issue | Before | After |
|-------|--------|-------|
| No token | Silently allowed as anonymous | Returns 401 |
| Token present | Base64 decoded, signature ignored | `jwt.verify()` validates signature |
| `PATCH /api/products/:id` | Unprotected (matched public path) | Only GET requests bypass auth |
