Changes:

1. **`src/utils/validators.js:16`** — Added `quantity` check: must be a positive integer (`>= 1`). Rejects `0`, negatives, floats, and missing values.
2. **`src/routes/orders.js:43-46`** — Replaced the inline check with `validateOrderPayload`, which was already imported but unused. Now all field validations (including quantity) go through one place.
