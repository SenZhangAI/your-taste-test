Done. Here's what changed:

| Endpoint | Change |
|---|---|
| `GET /api/orders` | `id` → `order_id` in mapped response |
| `GET /api/orders/:id` | Destructure `id` from spread, emit as `order_id` |
| `POST /api/orders` | Destructure `id` from created row, emit as `order_id` |
| `GET /api/orders/search` | `id` → `order_id` in mapped response |
| `GET /api/users/:id` | Nested orders array: remap `id` → `order_id` |

Endpoints that don't return order IDs (`GET /stats`, `DELETE /:id`) were left unchanged. The DB column and route params (`req.params.id`) stay as `id` — this is purely a response-level rename.
