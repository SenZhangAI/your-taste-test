Done. Summary of changes:

- **`order-service.js`** — `listOrders` queries use `.whereNull('deleted_at')`, `softDeleteOrder` sets `deleted_at` instead of `status: 'deleted'`
- **`routes/orders.js`** — route guards check `order.deleted_at` instead of `order.status === 'deleted'`
- **`validators.js`** — updated comment
- **`seed.js`** — soft-deleted seed rows now set `deleted_at` timestamp instead of `status: 'deleted'`

Note: `getOrder()` at line 32 doesn't filter out deleted orders — it relies on callers to check. This matches the existing pattern (both route handlers already check after calling it). If you want the service to handle it centrally, I can add `.whereNull('deleted_at')` there too.
