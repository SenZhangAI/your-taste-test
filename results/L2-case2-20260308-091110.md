Done. Here's a summary of all changes:

**`src/services/order-service.js`:**
- `listOrders` — both the query and count query now use `.whereNull('deleted_at')`
- `searchOrders` — same change
- `getOrder` — added `.whereNull('deleted_at')` so deleted orders are filtered at the source
- `softDeleteOrder` — sets `deleted_at` timestamp instead of `status: 'deleted'`

**`src/routes/orders.js`:**
- GET `/:id` and DELETE `/:id` — removed the now-redundant `order.status === 'deleted'` checks (since `getOrder` handles it)
- GET `/stats` — uses `.whereNull('deleted_at')` instead of status check
