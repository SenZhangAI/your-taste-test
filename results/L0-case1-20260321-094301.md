Done. The changes:

1. **`src/routes/orders.js:14`** — reads `req.query.category` and passes it to `listOrders`
2. **`src/services/order-service.js:10`** — accepts `category` param and adds a `.where('category', category)` clause when provided

Usage: `GET /api/orders?category=electronics` will return only orders where the `category` column equals `"electronics"`. Without the param, all (non-deleted) orders are returned as before.
