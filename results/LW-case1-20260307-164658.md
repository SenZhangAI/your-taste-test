Done. Here's what changed:

- **`src/routes/orders.js:12-13`** — reads `req.query.category` and passes it to `listOrders`
- **`src/services/order-service.js:10-22`** — accepts `category` param; when provided, adds `.where('category', category)` to both the data query and the count query (using `.clone()` to share the base filter)

Usage: `GET /api/orders?category=electronics` returns only orders with `category = 'electronics'`. Omitting the param returns all orders as before.
