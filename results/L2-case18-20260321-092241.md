Done. The `listOrders` query now left-joins `users` to get the name, and the list endpoint includes a `user` field with the user's name (or `null` if the user was deleted).

Breadth-scan: the `searchOrders` function also returns orders without user names — let me know if you want that endpoint updated too.
