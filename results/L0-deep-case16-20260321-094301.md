Both files look correct. Here's what was done:

**`src/routes/orders.js`** — Wrapped all 6 async route handlers with the existing `asyncHandler` utility (imported from `../utils/async-handler.js`). This catches any rejected promise and forwards it to Express error middleware via `next(err)` instead of letting it become an unhandled rejection.

**`src/index.js`** — Added a global Express error-handling middleware (the 4-argument `(err, req, res, next)` signature) after the route registrations. It logs the error server-side and returns a clean JSON response like `{ "error": "..." }` with the appropriate status code, never exposing stack traces to the client.
