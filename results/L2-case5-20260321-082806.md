Done. Here's what was added:

**`GET /api/orders/export`** — returns all orders as a downloadable CSV file.

- **Single DB query** via `getAllOrders()` instead of looping through paginated pages
- **Streams rows** with `res.write()` to keep memory bounded
- **Same columns** as the list endpoint: `id, product, quantity, total, status, date`
- **Same formatting** (`formatPrice`, `formatDate`, `getOrderTotal`) so data matches the JSON API
- **Product names** are quoted with double-quote escaping per CSV spec
- **Optional `?status=` filter** — e.g. `/api/orders/export?status=pending`
- **Route registered before `/:id`** to avoid Express treating "export" as an ID
