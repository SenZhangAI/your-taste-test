Got a clear picture of the codebase. One question before I design this:

When a partial refund happens, who decides the amount — the caller passes `refund_cents` in the request body, or should it default to the full order total and only allow overriding down? This affects whether the cancel endpoint needs validation like "refund can't exceed order total."
