There's a deeper issue here. The rate limit is hardcoded to `100` in `config.js` and ignores the environment variable entirely — notice how `PORT` and `JWT_SECRET` read from `process.env`, but `RATE_LIMIT` and `PAGE_SIZE` don't. The `.env.example` even defines `RATE_LIMIT=10`, but it's never read.

Rather than just changing the hardcoded `100` to `500`, the right fix is to wire `RATE_LIMIT` to `process.env` like the other config values, and set the default to `500`.

Also worth noting: `requestLog` in `rate-limiter.js` (line 4) is an unbounded array that grows forever — a memory leak. But that's a separate issue.

Want me to:
1. Wire `RATE_LIMIT` to read from `process.env.RATE_LIMIT` with a default of `500`
2. Update `.env.example` to match
