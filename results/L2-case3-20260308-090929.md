Done. To summarize what was wrong and what changed:

- **`config.js` was ignoring the env var** — `RATE_LIMIT` was hardcoded to 100. Now it reads `process.env.RATE_LIMIT` with 100 as fallback.
- **`.env.example` updated** from 10 to 500.

Make sure your actual `.env` file also has `RATE_LIMIT=500` — `.env.example` is just a template and doesn't affect runtime.
