That fixed all three call sites (list, get single, and search endpoints). The `/stats` endpoint on line 70 computes revenue inline and keeps it in cents, which is fine for an admin stats response — but you may want to consider whether that should also be converted to dollars for consistency.
