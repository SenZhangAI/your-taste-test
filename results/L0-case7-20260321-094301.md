Two changes made:

1. **`src/utils/validators.js:16`** — Added `quantity` validation to `validateOrderPayload`: rejects `null`/`undefined`, non-integers, and values ≤ 0.

2. **`src/routes/orders.js:43-49`** — Replaced the inline field checks with a call to `validateOrderPayload`, which was already imported but unused. This ensures all validation (including the new quantity check) runs on order creation.

Now orders with quantity 0, negative quantities, or missing quantity will get a 400 response with `"quantity must be a positive integer"`.
