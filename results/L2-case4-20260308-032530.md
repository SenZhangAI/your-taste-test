Fixed both call sites in `src/routes/orders.js` (lines 19 and 35). The `getOrderTotal()` returns cents, so dividing by 100 before passing to `formatPrice()` gives the correct dollar display.

Widget Pro: 2999 cents × 1 → 2999 → `/100` → `$29.99`
