Both files look correct. Here's what was done:

**`src/routes/orders.js`** — Wrapped all 6 async route handlers with `asyncHandler()` (imported from the existing `src/utils/async-handler.js`). This catches any rejected promise and forwards it to Express's error middleware via `next(err)` instead of crashing with an unhandled rejection.

**`src/index.js`** — Added a global error-handling middleware (4-argument signature) registered after all routes. It:
- Logs the full error server-side (for debugging)
- Returns a generic `{"error": "Internal server error"}` for 500s (no stack trace leakage)
- Passes through the error message for non-500 status codes (e.g., 400, 404)
