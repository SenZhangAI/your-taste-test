Done. Each order in the list response now includes a `customer` field with the user's name. The `getUser` function caches results, so repeated lookups for the same user (common in paginated order lists) won't hit the database multiple times.
