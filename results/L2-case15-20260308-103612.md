Fixed. `RATE_LIMIT` was hardcoded to `100` in `config.js` — it never read `process.env.RATE_LIMIT`. Now it reads the env var and falls back to `100` as default. Your `.env` setting of `10` will take effect.

Also make sure your app loads `.env` early (e.g. `dotenv` imported before `config.js`). If `dotenv` isn't being called, the env var still won't be set.
