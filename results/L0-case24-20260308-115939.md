Everything looks good. Here's a summary of the changes:

**Migration** (`migrations/004-add-order-cancellation.js`): Adds `refund_amount_cents` (integer, nullable) and `cancelled_at` (timestamp, nullable) columns to the `orders` table.

**Service** (`src/services/order-service.js`): Added `cancelOrder(id, refundAmountCents)` — sets status to `'cancelled'`, stores the refund amount and cancellation timestamp.

**Routes** (`src/routes/orders.js`):
- **`POST /:id/cancel`** — Cancels an order. Accepts optional `refund_amount_cents` in the body (defaults to full order total for a full refund). Validates the refund is between 0 and the order total. Returns 404 if not found, 400 if already cancelled or invalid refund amount.
- **`GET /:id`** — Now includes `refund_amount` and `cancelled_at` in the response when the order is cancelled.
