The bug was that `total_cents` wasn't in `SORTABLE_FIELDS`, so sorting by price silently fell back to `created_at` (date order). Since `total_cents` is stored as an integer in the DB, the database's `ORDER BY` will sort numerically — no string comparison issue.

Clients can now pass `sort=total_cents` to get proper price ordering.
