Done. Added:

- **`PATCH /orders/:id/status`** — accepts `{ "status": "confirmed" }` (or any value from `ORDER_STATUSES`: pending, confirmed, shipped, delivered, cancelled)
- Returns 400 for invalid status, 404 if order doesn't exist or is soft-deleted
- **`updateOrderStatus(id, status)`** in `order-service.js` — updates status + `updated_at` timestamp, returns the updated order

The `deleted` status is still only settable via the existing `DELETE` endpoint, keeping soft-delete separate from normal status transitions.
