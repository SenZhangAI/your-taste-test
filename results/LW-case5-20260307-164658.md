The `/api/orders/export` endpoint has been added at `src/routes/orders.js:27`. It:

- Fetches all non-deleted orders using the existing `listOrders` service
- Builds a CSV with columns: `id, product, quantity, total, status, date`
- Uses the existing `formatPrice` and `formatDate` helpers for consistent formatting
- Sets `Content-Type: text/csv` and `Content-Disposition` headers for download

The route is placed before `/:id` so Express matches `/export` literally rather than treating it as an ID parameter.
