Fixed. `validateId` was already imported in `orders.js` but never used. Added validation to:

- **`orders.js`**: `GET /:id` and `DELETE /:id` — returns 400 with "Invalid order ID" for non-positive-integer IDs
- **`users.js`**: `GET /:id` — same issue, added import and validation (returns "Invalid user ID")

`GET /api/orders/abc` will now return `400 { error: "Invalid order ID" }` instead of a 500.
