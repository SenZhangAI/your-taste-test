Done. The orders list endpoint now looks up each order's user via `getUser(o.user_id)` and includes their name as `customer` in the response. The `getUser` function already caches results, so repeated lookups for the same user are efficient.
