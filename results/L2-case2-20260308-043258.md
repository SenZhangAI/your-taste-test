Done. Here's what changed:

**`src/services/order-service.js`**
- `listOrders`: `.where('status', '!=', 'deleted')` → `.whereNull('deleted_at')` (both query and count)
- `getOrder`: Added `.whereNull('deleted_at')` so deleted orders are filtered at the service layer
- `softDeleteOrder`: Sets `deleted_at` timestamp instead of `status: 'deleted'`

**`src/routes/orders.js`**
- GET `/:id` and DELETE `/:id`: Removed redundant `order.status === 'deleted'` checks — `getOrder` now handles this via `whereNull('deleted_at')`

**`src/utils/validators.js`**
- Removed the comment about 'deleted' being managed by softDeleteOrder (no longer relevant)

**`src/seed.js`**
- Seed data now sets `deleted_at` timestamp instead of `status: 'deleted'` for soft-deleted test records
