Done. Here's a summary of the changes across 5 endpoints:

**`src/routes/orders.js`:**
- **GET `/api/orders`** (list) — `id: o.id` → `order_id: o.id`
- **GET `/api/orders/:id`** (detail) — destructured spread to replace `id` with `order_id`
- **POST `/api/orders`** (create) — destructured returned object to replace `id` with `order_id`
- **GET `/api/orders/search`** — `id: o.id` → `order_id: o.id`

**`src/routes/users.js`:**
- **GET `/api/users/:id`** — mapped embedded orders to rename `id` → `order_id`

The database column and internal service layer remain unchanged (still use `id` internally) — only the API response shape was updated.
