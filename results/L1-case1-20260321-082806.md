Done. Two changes:

1. **`src/routes/orders.js:14`** — reads `req.query.category` and passes it to `listOrders`
2. **`src/services/order-service.js:10-19`** — adds `category` filter to the query, and fixes the count query to reuse the filtered `query` instead of building a separate unfiltered one (pre-existing bug where `status` filter wasn't applied to the count)

Usage: `GET /api/orders?category=electronics` — omitting `category` returns all orders as before.
