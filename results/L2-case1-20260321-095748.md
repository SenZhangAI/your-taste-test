Done. Two changes:

1. **`src/routes/orders.js:14`** — reads `category` from query params and passes it to `listOrders`
2. **`src/services/order-service.js:17-19`** — filters by `category` when provided

Usage: `GET /api/orders?category=electronics`

I also fixed a pre-existing bug in the count query: it previously ignored the `status` and `category` filters, so pagination totals were wrong when filtering. Now the count query reuses the same filtered base query.
