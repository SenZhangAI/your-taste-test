Changes:

1. **`src/utils/validators.js:16`** — Added `quantity` to `validateOrderPayload`; rejects if not a positive integer (`0`, negative, `null`, non-integer all fail).

2. **`src/routes/orders.js:43-46`** — Replaced the ad-hoc field check with `validateOrderPayload()`, which was already imported but unused. Now all field validation is in one place.
