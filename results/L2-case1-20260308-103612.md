Done. Two changes:

1. **`src/routes/orders.js:14`** — reads `?category=` from query params and passes it to `listOrders`
2. **`src/services/order-service.js:10`** — accepts `category`, adds `.where('category', category)` when present

Also fixed a pre-existing bug: the total count query now uses the same filtered `query` base instead of an unfiltered `db('orders')`, so pagination totals are correct when filtering by `status` or `category`.
