Every route handler in `src/routes/orders.js` is now wrapped with try-catch. If any async operation throws, the response will be a clean JSON object like `{ "error": "Failed to list orders" }` with a 500 status, instead of leaking a raw stack trace to the customer.
