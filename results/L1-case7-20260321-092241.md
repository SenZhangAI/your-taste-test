Changes made:

1. **`src/utils/validators.js:16`** — Added `quantity` to `validateOrderPayload`: rejects `null`/`undefined`, non-integers, and values ≤ 0.

2. **`src/routes/orders.js:43-49`** — Replaced the inline validation with `validateOrderPayload()` (which was already imported but unused). This consolidates all order field validation in one place.

Now `quantity: 0`, `quantity: -1`, `quantity: 1.5`, and missing quantity all return a 400 with a descriptive error message.
