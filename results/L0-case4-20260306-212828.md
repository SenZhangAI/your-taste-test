Fixed. The issue was at `src/services/order-service.js:36` — `getOrderTotal()` was returning a value in cents, but `formatPrice()` expects dollars. Added `/ 100` to convert cents to dollars before formatting.

Now a $29.99 widget (2999 cents) × qty 1 will correctly display as `$29.99` instead of `$2999.00`.
