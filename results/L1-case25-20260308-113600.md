The count query now clones the same `query` that already has both the `!= 'deleted'` and the optional `status` filter applied. When no status filter is passed, behavior is unchanged (counts all non-deleted). When filtering by status, the total will correctly reflect only matching orders.
