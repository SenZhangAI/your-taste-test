Fixed. The problem was **route ordering**, not the LIKE query. Express matches routes top-down, and `/:id` was catching `/search` (and `/stats`) by treating the path segment as an ID parameter.

Both `/search` and `/stats` are now defined before `/:id`, so they match first. The final order is: `/` → `/search` → `/stats` → `/:id` → `DELETE /:id`.
