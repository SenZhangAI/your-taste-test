All 6 route handlers in `src/routes/orders.js` now have try-catch blocks. Each catch:

1. Logs the full error to the server console (for debugging)
2. Returns a clean `{ "error": "..." }` JSON response with a 500 status — no stack trace exposed to the customer
