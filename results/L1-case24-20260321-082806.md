Everything looks good. Here's a summary of the changes:

**New file:**
- `migrations/004-add-cancellation-fields.js` — adds `refund_cents` and `cancelled_at` columns

**Modified files:**

- **`src/db.js`** — auto-applies migration 004 on init (consistent with existing pattern)
- **`src/services/order-service.js`** — new `cancelOrder(id, refundCents)` function:
  - Only `pending`/`confirmed` orders can be cancelled (409 otherwise)
  - Validates `refund_cents` is between 0 and order total
  - Sets status to `cancelled`, records refund amount and timestamp
- **`src/routes/orders.js`**:
  - `POST /api/orders/:id/cancel` — accepts `{ refund_cents }` body, returns cancelled order with refund info
  - `GET /api/orders/:id` — now includes `refund` and `cancelled_at` for cancelled orders
  - `GET /api/orders` (list) — same refund fields on cancelled orders

**Validation rules:**
- `refund_cents` is required (integer, in cents)
- Must be `0 ≤ refund_cents ≤ order total` (supports partial refunds)
- Only cancellable from `pending` or `confirmed` status
