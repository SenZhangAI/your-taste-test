Done. Changed `id` → `order_id` in all 5 order response locations:

- **`src/routes/orders.js`**: List endpoint (line 18), get single (line 35), create (line 49), search (line 58)
- **`src/routes/users.js`**: User's orders in get-user endpoint (line 26)

The DB column and route params (`/:id`, `req.params.id`) are unchanged — only the JSON response field name was renamed.
