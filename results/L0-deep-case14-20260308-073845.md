The POST handler now formats the response the same way as the GET `/:id` handler — adding `total` (via `formatPrice(getOrderTotal(order))`) and `date` (via `formatDate(order.created_at)`) to the returned order object.
