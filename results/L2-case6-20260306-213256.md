Fixed. `getOrder` now filters deleted records at the service layer, matching its JSDoc contract.

The route-level check at `src/routes/orders.js:28` (`order.status === 'deleted'`) is now redundant but harmless as defense-in-depth. No other callers are affected — `createOrder` creates with status `'pending'`, and the delete route's `!order` check handles the null return correctly.
