Done. Here's what changed:

**`src/routes/orders.js`** — All 4 route handlers wrapped with `asyncHandler()`, which catches rejected promises and forwards them to `next(err)` instead of crashing with an unhandled rejection.

**`src/index.js`** — Added a global error-handling middleware (after all routes) that:
- Logs the full error server-side via `console.error`
- Returns a clean JSON response: `{ "error": "Internal server error" }` for 500s
- Uses `err.message` for errors with an explicit status code (e.g. 400, 404)
- Never exposes stack traces to the client
