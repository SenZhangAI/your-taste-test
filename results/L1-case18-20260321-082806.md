Each order in the list response now includes a `customer` field with the user's name. The implementation:

- Collects unique `user_id` values from the page of orders
- Fetches them in parallel via `getUser` (which caches, so subsequent pages with the same users hit cache)
- Falls back to `null` if a user isn't found (e.g. deleted user)

Response shape change:
```json
{
  "id": 1,
  "customer": "Jane Doe",   // ← new
  "product": "Widget",
  "quantity": 2,
  "total": "$19.98",
  "status": "pending",
  "date": "2026-03-21"
}
```

**Breadth-scan:** The search endpoint (`GET /api/orders/search`, line 53) also returns orders without user info. Want me to add customer names there too?
