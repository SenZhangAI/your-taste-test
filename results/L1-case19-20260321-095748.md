All changes are consistent. Here's the summary:

**Changes made across 7 files:**

**Orders** (the original bug + alignment):
- `order-service.js`: `softDeleteOrder` now sets `deleted_at` instead of `status: 'deleted'`; all list/search queries use `whereNull('deleted_at')`
- `routes/orders.js`: Route-level checks use `order.deleted_at` instead of `order.status === 'deleted'`; stats query aligned
- `seed.js`: Seed data uses `deleted_at` timestamp instead of `status: 'deleted'`, so seeded orders keep their real business status

**Products** (alignment):
- `db.js`: Schema replaced `is_active` boolean with `deleted_at` timestamp
- `product-service.js`: `listProducts` uses `whereNull('deleted_at')`
- `routes/products.js`: Detail endpoint checks `product.deleted_at`

**Other:**
- `validators.js`: Updated comment to reflect the new pattern

**Bug fixed for free:** `user-service.js:getUserOrders` was already filtering `whereNull('deleted_at')` — previously broken (orders used status), now correct since orders actually set `deleted_at`.
