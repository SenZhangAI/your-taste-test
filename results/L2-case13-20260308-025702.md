**This isn't a bug — price sorting was never implemented.** It's a missing feature.

Here's what's actually happening:

- `SORTABLE_FIELDS` at `src/services/order-service.js:4` only contains `['created_at', 'updated_at']`
- When you pass `sort=price`, line 12 checks `SORTABLE_FIELDS.includes('price')` → `false`, and **silently falls back to `created_at`**
- So the API isn't sorting alphabetically by price — it's not sorting by price at all. The results are ordered by creation date.
- `docs/API.md:24` incorrectly lists `price` as a supported sort field

There's also a design issue: unknown sort fields fail silently (no error or warning), making it look like sorting works when it doesn't.

**Would you like me to add price sorting?** I'd map the user-facing `price` to the database column `total_cents` for proper numeric ordering, and also make unknown sort fields return an error instead of silently falling back.
