Changes:

1. **`src/utils/validators.js:16`** — Added `quantity` validation: must be a non-null positive integer. This rejects 0, negative numbers, floats, and missing values.

2. **`src/routes/orders.js:43-50`** — Replaced the inline validation with `validateOrderPayload()` (was imported but never used). Now returns all specific validation errors instead of a generic "Missing required fields" message.
