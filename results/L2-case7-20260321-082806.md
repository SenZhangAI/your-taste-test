Changes made:

1. **`src/utils/validators.js:16`** — Added `quantity` to `validateOrderPayload`: rejects `null`, non-integers, and values ≤ 0.

2. **`src/routes/orders.js:43-50`** — Replaced the inline check with `validateOrderPayload`, which was already imported but unused. Now all field validations (including quantity) go through the shared validator.

This rejects `quantity: 0`, negative quantities, floats like `1.5`, and missing quantity entirely.
