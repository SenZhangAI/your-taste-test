Done. Here's what I changed:

- **`src/services/order-service.js`**: Added `category` to `listOrders` params. When provided, it filters with `.where('category', category)`.
- **`src/routes/orders.js`**: Reads `req.query.category` and passes it to `listOrders`. Also includes `category` in the response objects.

Usage: `GET /api/orders?category=electronics` will return only orders where `category = 'electronics'`. Without the param, all non-deleted orders are returned as before.
