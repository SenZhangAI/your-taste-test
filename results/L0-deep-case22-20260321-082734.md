The LIKE query in `order-service.js:37` is correct. The problem was purely route precedence — Express saw `/:id` first and treated `"search"` as an order ID. Moving `/search` above `/:id` fixes it. Same applies to the `/stats` route on line 66 — it has the same vulnerability but would return a different error since it doesn't call `getOrder`.
