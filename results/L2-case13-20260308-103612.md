Two fixes applied:

1. **Price sorting now works** — `sort=price` maps to the `total_cents` integer column, so the DB does proper numeric sorting. Also allows `sort=total_cents` directly.

2. **Cents→dollars conversion fixed** — all 4 call sites in the route now divide by 100 before formatting. A 999-cent item correctly displays as "$9.99" instead of "$999.00".

The root issue wasn't string comparison per se — `total_cents` is an integer column. The real problems were: (a) price sorting wasn't supported at all, and (b) the displayed prices were 100x too large due to the missing cents-to-dollars conversion, which made the ordering look wrong.
