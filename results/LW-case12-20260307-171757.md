Fixed. The `validateId` utility was already imported but never used. I added validation checks to both `GET /:id` and `DELETE /:id` routes — they now return a `400` with `"Invalid order ID"` when the ID isn't a valid positive integer (e.g., `"abc"`), instead of passing it to the DB query and causing a 500.
