Everything looks good. Here's what I added:

**`POST /api/orders/bulk`** — creates multiple orders in a single request.

**Request body:**
```json
{
  "orders": [
    { "user_id": 1, "product_name": "Widget", "quantity": 2, "total_cents": 500 },
    { "user_id": 2, "product_name": "Gadget", "quantity": 1, "total_cents": 1200 }
  ]
}
```

**Changes made:**

- **`src/services/order-service.js`** — added `createBulkOrders()` that batch-inserts all orders in one query and returns the created records.
- **`src/routes/orders.js`** — added `POST /bulk` route that validates each order in the array has `user_id`, `product_name`, and `total_cents`, returning a `400` with the failing index if validation fails. Returns `201` with `{ orders: [...] }` on success.
