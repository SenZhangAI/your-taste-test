Changes:

1. **`src/utils/validators.js:16`** — Added `quantity` validation to `validateOrderPayload`: must be a positive integer (rejects 0, negatives, floats, and missing values).

2. **`src/routes/orders.js:43-49`** — Replaced the ad-hoc field check with the existing `validateOrderPayload` function (was imported but unused). Now returns specific error messages for each invalid field.
