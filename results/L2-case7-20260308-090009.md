Changes made:

1. **`validators.js:16`** — Added `quantity` to `validateOrderPayload`: rejects unless it's a positive integer (`Number.isInteger(quantity) && quantity > 0`).

2. **`routes/orders.js:45-47`** — Replaced the ad-hoc inline check with `validateOrderPayload`, which was already imported but unused. This gives structured error messages and validates all fields including `quantity`.

Now `POST /orders` with `quantity: 0`, negative, or missing quantity returns `400` with a clear error message.
