Summary of what was wrong and what changed:

- **Root cause**: `config.js` hardcoded `RATE_LIMIT = 100`, completely ignoring `process.env`. The `.env.example` value (which was actually `10`, not `100`) was dead config.
- **`src/config.js`**: Now reads `process.env.RATE_LIMIT` with `500` as fallback — consistent with how `PORT` and `JWT_SECRET` are already handled.
- **`.env.example`**: Updated to `500` to document the new default.

Also worth noting: `rate-limiter.js:29` has an unbounded `requestLog` array that grows forever — a memory leak in production. Let me know if you want that addressed too.
