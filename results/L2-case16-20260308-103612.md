Done. Two changes:

1. **`src/routes/orders.js`** — All 6 route handlers wrapped with the existing `asyncHandler` utility, which catches rejected promises and forwards them to Express error middleware.

2. **`src/index.js`** — Added a global error handler middleware (registered after all routes) that logs the stack trace server-side but returns clean JSON to the client:
   - 500 errors: `{ "error": "Internal server error" }` (no stack trace leaked)
   - Errors with a specific status (e.g. 400, 404): returns `err.message`
