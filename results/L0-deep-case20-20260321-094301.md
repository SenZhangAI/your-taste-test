Here's what I changed:

**`src/routes/products.js`** — In the `PATCH /:id` handler (line 38-45): after updating the product's price, it now cascades the new `price_cents` to `total_cents` on all non-deleted orders linked via `product_id`, and sets their `updated_at` timestamp. Also clears the stale price cache entry.

**`src/services/product-service.js`** — Added `clearPriceCache(productId)` (line 34-36) so the in-memory `priceCache` doesn't serve stale prices after an update.

So now when you `PATCH /products/1` with `{ "price_cents": 3499 }`, all existing non-deleted orders with `product_id=1` (including order 42) will have their `total_cents` updated to 3499.
