Done. `GET /api/orders?category=electronics` now filters orders by the `category` column. When the parameter is omitted, all non-deleted orders are returned as before.

Key changes:
- **`src/routes/orders.js:12-13`** — reads `category` from query params and passes it to `listOrders`
- **`src/services/order-service.js:10-18`** — builds a base query with the optional `category` filter, then clones it for both the data fetch and the count query
