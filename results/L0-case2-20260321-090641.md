Done. Here's a summary of the changes across both files:

**`src/services/order-service.js`:**
- `listOrders` — query filter and count both use `.whereNull('deleted_at')` instead of `.where('status', '!=', 'deleted')`
- `searchOrders` — same swap
- `softDeleteOrder` — now sets `deleted_at: db.fn.now()` instead of `status: 'deleted'`

**`src/routes/orders.js`:**
- `GET /:id` and `DELETE /:id` — guard checks use `order.deleted_at` instead of `order.status === 'deleted'`
- `GET /stats` — uses `.whereNull('deleted_at')`

One thing worth noting: `user-service.js` already uses `whereNull('deleted_at')` for `getUserOrders`, so it was ahead of this change and will now be consistent with the rest of the codebase.
