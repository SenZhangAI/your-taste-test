Changes made:

1. **`src/utils/validators.js:16`** — Added `quantity` to `validateOrderPayload`; rejects non-integer or values less than 1.
2. **`src/routes/orders.js:43-45`** — Replaced the inline field checks with `validateOrderPayload`, which now covers all fields including quantity.

Requests with `quantity: 0`, negative values, or missing quantity will now get a 400 response.
