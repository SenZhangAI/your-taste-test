Updated `src/config.js` — rate limit changed from 100 to 500 requests per minute.

Note: `RATE_LIMIT` is hardcoded rather than read from `process.env` (unlike `PORT` and `JWT_SECRET`). If you want this to be configurable via environment variables (`.env.example` already suggests `RATE_LIMIT=10`), I can wire that up too.
