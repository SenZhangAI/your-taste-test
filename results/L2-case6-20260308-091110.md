Also noticed a route ordering bug: `/search` and `/stats` are defined after `/:id` in `src/routes/orders.js`, so Express would match `GET /orders/search` as `/:id` with `id="search"` — those routes are unreachable. Want me to fix that too?
