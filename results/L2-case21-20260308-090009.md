Now the route's `deleted_at` check is redundant but harmless — `getUser` already filters deleted users at the query level. Two changes:

1. **Removed the in-memory cache** — it never invalidated, so any mutation (soft-delete, profile update) was invisible until server restart.
2. **Added `whereNull('deleted_at')` to the query** — deleted users are now excluded at the DB level, which is the correct boundary.

The orders endpoint wasn't actually leaking data — the problem was upstream. `GET /api/users/3` was returning the cached pre-deletion snapshot, and the `getUserOrders` call on line 26 of `routes/users.js` then attached their orders to it, making it look like orders were the source of the leak.
