Here's what I changed in `src/services/order-service.js`:

1. **`listOrders`** — both the main query filter (line 14) and the pagination count query (line 26) now use `.whereNull('deleted_at')` instead of `.where('status', '!=', 'deleted')`
2. **`searchOrders`** — same filter swap (line 38)
3. **`softDeleteOrder`** — now sets `deleted_at: db.fn.now()` instead of `status: 'deleted'`

One thing to note: `getOrder(id)` doesn't filter out soft-deleted rows — it returns any order by ID regardless. Let me know if you want that to also check `whereNull('deleted_at')`.
