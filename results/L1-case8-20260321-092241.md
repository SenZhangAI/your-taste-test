**What changed:**
1. No token → **401 rejected** instead of silently passing through as anonymous
2. Token present → **`jwt.verify()` validates the signature** against `JWT_SECRET` instead of just base64-decoding the payload
3. Invalid/expired token → **401 rejected** instead of falling through to anonymous

**Breadth-scan — what else to check:**

- The routes (`orders.js`, `users.js`) may assume `req.user` always exists (since it used to be set for anonymous). Now `req.user` is only set for authenticated requests, but those routes are behind the middleware so they'll always have it.
- `config.js:3` — the fallback `'dev-secret-do-not-use'` is fine for local dev, but make sure `JWT_SECRET` is set in prod env (you said it is, so you're good).
- `/api/products` is public by design — that still works unchanged.
