Everything works. Here's what I added:

**Service** (`src/services/order-service.js`): `getAllOrders({ sort, status })` — queries all non-deleted orders in one shot (no pagination), reusing the same sort/status filtering as `listOrders`.

**Route** (`src/routes/orders.js`): `GET /api/orders/export` — calls `getAllOrders`, formats each row with the same fields as the list endpoint (id, product, quantity, total, status, date), and returns a CSV file download. It supports optional `?sort` and `?status` query params. Product names are properly CSV-escaped (double-quoted with internal quotes doubled).

Rather than looping through pages of our own API, we just query the DB directly without a limit — simpler and avoids the overhead of repeated HTTP-style pagination.
