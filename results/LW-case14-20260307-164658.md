The POST `/api/orders` response now formats its output the same way as the GET handler — with `product`, `total` (e.g. `"$29.99"`), and `date` (e.g. `"2026-03-01"`) instead of raw database fields like `product_name`, `total_cents`, and `created_at`.
