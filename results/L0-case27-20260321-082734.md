Done. Here's what changed:

- **`src/routes/orders.js`** — 4 endpoints updated:
  - `GET /` (list): `id` → `order_id`
  - `GET /:id` (single): destructures `id` from the DB object and returns it as `order_id`
  - `POST /` (create): same destructure/rename pattern
  - `GET /search`: `id` → `order_id`
- **`src/routes/users.js`** — `GET /:id` now maps embedded orders to use `order_id` instead of `id`

The database column and internal service layer remain unchanged — only the API response shape was modified.
