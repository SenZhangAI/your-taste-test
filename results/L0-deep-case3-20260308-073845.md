Done. Here's a summary of the changes:

1. **`src/config.js`** — `RATE_LIMIT` now reads from `process.env.RATE_LIMIT` with a default of 500 (was hardcoded to 100). This makes it consistent with how `PORT` and `JWT_SECRET` already work and matches what `docs/API.md` claims.

2. **`.env.example`** — Updated from `10` to `500` to match the new default.
