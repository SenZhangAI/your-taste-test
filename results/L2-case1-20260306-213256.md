Done. `GET /api/orders?category=electronics` now filters by the `category` column. The base query is built once and `.clone()`d for both the data fetch and the count, so the filter applies consistently to both.
