Here's what I changed:

1. **`src/routes/orders.js`** — Imported the existing `asyncHandler` utility and wrapped all 4 route handlers (`GET /`, `GET /:id`, `POST /`, `DELETE /:id`) with it. This catches any rejected promise and forwards the error to Express's error middleware instead of letting it crash with a raw stack trace.

2. **`src/index.js`** — Added an error-handling middleware (4-argument `(err, req, res, next)`) right before `initDB()`. It logs the error server-side and returns a clean JSON response: `{ "error": "<message>" }` with the appropriate status code (defaulting to 500).
