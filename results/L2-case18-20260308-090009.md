Done. The `listOrders` query now joins `users` via `LEFT JOIN` on `user_id` to pull `users.name as user_name`, and the list endpoint includes it as `user` in each order object. Used a left join so orders with invalid/missing user_ids still appear (with `user: null`).
