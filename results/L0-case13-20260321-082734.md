This isn't actually a bug with string-vs-numeric comparison. **Price sorting was never implemented.** Here's what's happening:

- `SORTABLE_FIELDS` in `order-service.js:4` only allows `['created_at', 'updated_at']`
- When you pass `sort=price`, line 12 falls back to `created_at` because `'price'` isn't in the allowed list
- The API docs (`docs/API.md`) incorrectly list `price` as a supported sort value
- The orders appear in `created_at` descending order, which can look like a price-sorting bug

The orders table stores `total_cents` as an integer column, so there's no string comparison issue — price sorting just doesn't exist yet.

**Two options:**

1. **Add price sorting** — add `total_cents` to `SORTABLE_FIELDS` and map `sort=price` to it
2. **Fix the docs** — remove `price` from the documented sort values

Want me to implement price sorting, or just fix the docs?
