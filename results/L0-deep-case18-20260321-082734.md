Done. The orders list endpoint now includes a `customer` field with the user's name for each order. The `getUser` function has built-in caching, so repeated lookups for the same user (e.g., if one user placed many orders) won't hit the database multiple times.
