Done. Two things changed:

1. **Default bumped to 500** as requested.
2. **Now reads `process.env.RATE_LIMIT`** — the config was ignoring the env var entirely (`.env.example` had `RATE_LIMIT=10` but it was never wired up). This means you can tune the limit per environment without code changes.

Note: `requestLog` in `rate-limiter.js:4` is an unbounded array that grows forever — it's a memory leak. Every request pushes to it and nothing ever trims it. Worth addressing separately if this service runs long-lived.
