The fix: `src/config.js:2` had `RATE_LIMIT` hardcoded to `100`, ignoring the env var entirely. It now reads `process.env.RATE_LIMIT` (falling back to `100` if unset), matching the pattern already used for `PORT` and `JWT_SECRET` in the same file.
