Done. Here's the summary of changes:

**`src/services/order-service.js`:**
- `listOrders` query + count: `.where('status', '!=', 'deleted')` → `.whereNull('deleted_at')`
- `searchOrders`: same change
- `softDeleteOrder`: sets `deleted_at` instead of `status: 'deleted'`

**`src/routes/orders.js`:**
- GET `/:id` and DELETE `/:id`: `order.status === 'deleted'` → `order.deleted_at`
- GET `/stats`: `.where('status', '!=', 'deleted')` → `.whereNull('deleted_at')`

One thing to note: `getOrder()` still returns all orders including soft-deleted ones (the route layer handles the check). You may want to add `.whereNull('deleted_at')` there too so the filter is consistent at the service layer — but that would change the behavior of `softDeleteOrder` which calls `getOrder` after insert. Let me know if you want that change.
