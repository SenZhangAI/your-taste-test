The `validateId` function was already imported but unused. Now both `GET /:id` and `DELETE /:id` validate the param upfront — `GET /api/orders/abc` will return a `400 { "error": "Invalid order ID" }` instead of crashing with a 500.
