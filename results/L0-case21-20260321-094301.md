The `userCache` Map never invalidated entries, so once a user was fetched before deletion, the stale (non-deleted) version was served forever. Removing the cache ensures `getUser` always reflects the current DB state, so the `deleted_at` check in the route works correctly.
