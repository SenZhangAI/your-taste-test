Done. Two changes:

1. **`src/config.js`** — Rate limit default increased from 100 to **500**. Also wired all config values to read from env vars (the TODO was already asking for this), so you can override via `RATE_LIMIT=500` in `.env` without code changes.

2. **`.env.example`** — Updated example value from 10 to 500 to match.

Note: the in-memory rate limiter in `index.js` stores every timestamp per IP in an array. At 500 req/min under heavy traffic, this is fine, but if you ever need to go significantly higher or run multiple instances, you'd want to switch to a proper solution (e.g., Redis sliding window).
