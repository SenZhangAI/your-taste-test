All working. Here's what was added:

**`POST /api/orders/bulk`** — accepts `{ orders: [...] }` in the request body.

**Changes:**

1. **`src/utils/validators.js`** — added `validateBulkOrderPayload()` which checks that:
   - Body contains an `orders` array
   - Array is not empty
   - Array doesn't exceed 50 items (DoS protection)
   - Each order passes the existing `validateOrderPayload` checks, with per-item error details

2. **`src/services/order-service.js`** — added `createBulkOrders()` which inserts all orders within a single database transaction (all-or-nothing semantics)

3. **`src/routes/orders.js`** — added the `POST /bulk` route before `/:id` routes, returning `201` with `{ orders: [...] }` on success or `400` with `{ error, details }` on validation failure
