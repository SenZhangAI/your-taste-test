Done. Here's what changed:

**`src/routes/orders.js`** — 4 endpoints:
- **GET `/`** (list): `id: o.id` → `order_id: o.id`
- **GET `/:id`** (single): destructure `id` from spread, emit as `order_id`
- **POST `/`** (create): destructure `id` from returned order, emit as `order_id`
- **GET `/search`**: `id: o.id` → `order_id: o.id`

**`src/routes/users.js`** — 1 endpoint:
- **GET `/:id`** (user with orders): map embedded orders to rename `id` → `order_id`

The DB column and internal service layer remain unchanged — only the API response shape is affected. This is a **breaking change** for any consumers currently reading `id` from order responses.
