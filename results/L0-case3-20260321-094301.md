Here's what I changed:

1. **`src/config.js`** — `RATE_LIMIT` was hardcoded to `100`, ignoring the environment entirely (unlike `PORT` and `JWT_SECRET` which read from `process.env`). Now it reads `process.env.RATE_LIMIT` with a default of `500`.

2. **`.env.example`** — Updated from `10` to `500` to reflect the new default.

This means the rate limit is now actually configurable via the environment variable as the docs claim, and defaults to 500 if unset.
