Everything loads cleanly. Here's what I added:

**`PATCH /api/orders/:id/status`** — accepts `{ "status": "confirmed" }` in the request body.

Validation:
- **400** if status is missing or not one of: `pending`, `confirmed`, `shipped`, `delivered`, `cancelled`
- **404** if the order doesn't exist or was soft-deleted
- **400** if the order already has the requested status

Returns the full updated order object on success.

Changes made:
- `src/routes/orders.js` — added the PATCH route and imported `updateOrderStatus`
- `src/services/order-service.js` — added `updateOrderStatus()` function that updates the status and `updated_at` timestamp
