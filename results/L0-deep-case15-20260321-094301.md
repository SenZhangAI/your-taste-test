Fixed. The `RATE_LIMIT` was hardcoded to `100`, completely ignoring `process.env.RATE_LIMIT`. It now reads the env var (with `parseInt` since env vars are strings) and falls back to `100` if unset. With `RATE_LIMIT=10` in your `.env`, it will now correctly enforce 10 requests per window.
