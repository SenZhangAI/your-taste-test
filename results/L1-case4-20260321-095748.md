Fixed all 3 call sites (list, get-by-id, search). 

**Breadth-scan:** The `/stats` endpoint also computes `o.total_cents * o.quantity` but keeps it as a raw cents number for `revenue` — that's internally consistent (not passed to `formatPrice`), so no bug there. Though worth noting: the stats revenue is in cents while product prices display in dollars — if that revenue number ever gets displayed, it'll need the same `/100` conversion.
