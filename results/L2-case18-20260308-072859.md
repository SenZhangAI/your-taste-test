Done. The orders list now joins the `users` table and includes a `user` field with the user's name. Used a `leftJoin` so orders with a missing/deleted user still appear (with `user: null`).

Changes:
- `src/services/order-service.js:14-19` — added `leftJoin` on `users` and selected `users.name as user_name`
- `src/routes/orders.js:17` — added `user: o.user_name || null` to the response
