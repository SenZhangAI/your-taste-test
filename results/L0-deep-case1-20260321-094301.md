Done. Two changes made:

1. **`src/routes/orders.js:14`** — Extract `category` from `req.query` and pass it to `listOrders`.
2. **`src/services/order-service.js:10-28`** — Accept `category` param, add a `.where('category', category)` filter when present, and fixed the total count query to use the filtered `query` instead of an unfiltered base query (so pagination counts reflect the category filter).

Usage: `GET /api/orders?category=electronics` — returns only orders with `category = 'electronics'`. The parameter is optional; omitting it returns all orders as before.
