Changes:

- **`src/utils/validators.js:16`** — Added `quantity` to `validateOrderPayload`; rejects values that aren't positive integers (catches 0, negative, decimals, null/undefined)
- **`src/routes/orders.js:43-50`** — Replaced the inline check with the existing `validateOrderPayload` function (was imported but unused), which now covers all fields including quantity
