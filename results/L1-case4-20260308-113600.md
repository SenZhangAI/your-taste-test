All three call sites fixed. The `/stats` endpoint (line 71) also uses `total_cents * quantity` directly for revenue — that one's fine since it's accumulating raw cents for an admin stat, not displaying a formatted price.

**Root cause**: `getOrderTotal` returns cents, `formatPrice` expects dollars, and the route layer composed them without the `/ 100` conversion. Each function's contract was individually correct — the bug was purely at the integration boundary.
