All changes are clean. Here's what was done:

**Changes summary:**

| File | Change |
|------|--------|
| `src/services/order-service.js` | `softDeleteOrder` now sets `deleted_at`; all queries filter by `whereNull('deleted_at')` instead of `status != 'deleted'` |
| `src/routes/orders.js` | Single-order checks use `order.deleted_at` instead of `order.status === 'deleted'`; stats query uses `whereNull('deleted_at')` |
| `src/services/product-service.js` | `listProducts` filters by `whereNull('deleted_at')` instead of `is_active`; added `softDeleteProduct()` |
| `src/routes/products.js` | Product check uses `product.deleted_at` instead of `product.is_active` |
| `src/db.js` | Products schema: replaced `is_active` boolean with `deleted_at` timestamp |
| `migrations/002-products-soft-delete.js` | New migration to add `deleted_at` to products, migrate `is_active=false` data, drop `is_active` |

**Bug fixed**: `user-service.js:getUserOrders()` was already filtering by `whereNull('deleted_at')`, but `softDeleteOrder` never set it. Now it does — so deleted orders are properly excluded.

**Pattern is now consistent** across all three entities: users, orders, and products all use `deleted_at` timestamp for soft-delete.
