All done. Here's what changed:

**`src/routes/orders.js`** — renamed `id` → `order_id` in all 4 response points:
- `GET /api/orders` (list) — line 18
- `GET /api/orders/:id` (detail) — line 35-38, destructures `id` out of the spread
- `POST /api/orders` (create) — line 50-51, destructures `id` from returned object
- `GET /api/orders/search` — line 60

**`src/routes/users.js`** — line 26-27, remaps `id` → `order_id` on orders returned via `GET /api/users/:id`

No changes to the service layer or DB — the rename is purely at the API response level. The `/stats` and `DELETE` endpoints don't return order IDs so they're unaffected.
