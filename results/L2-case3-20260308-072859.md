Done. Two issues fixed:

1. **`src/config.js`** — `RATE_LIMIT` was hardcoded at `100`, ignoring the env var entirely (unlike `PORT` and `JWT_SECRET` which read from `process.env`). Now reads from `process.env.RATE_LIMIT` with `500` as the default.

2. **`.env.example`** — Updated from `10` to `500` to match the new default.

Note: the docs at `docs/API.md:54` already claim it's "Configurable via `RATE_LIMIT` environment variable" — that was misleading before but is now accurate. If you have a `.env` file in production, make sure to set `RATE_LIMIT=500` there too (or remove it to pick up the new default).
