`softDeleteOrder` now sets `deleted_at: db.fn.now()` alongside the existing `status` and `updated_at` updates. The rest of the codebase filters on `status`, so no other changes are needed — `deleted_at` records when the deletion happened.
