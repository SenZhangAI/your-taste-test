No tests configured. Here's a summary of all fixes:

**Critical fixes (your main memory problem):**

1. **`rate-limiter.js:requestLog`** — every request was appended forever. Now capped at 1000 entries (circular buffer via `shift()`). `getRequestStats()` only used the last 100 anyway.

2. **`logger.js:recentErrors`** — every error response was appended forever. Now capped at 100 entries. `getRecentErrors()` only returned the last 50 anyway.

**Secondary fixes (slower growth, still unbounded):**

3. **`user-service.js:userCache`** — cached users indefinitely. Added 5-min TTL so stale/deleted users get evicted.

4. **`product-service.js:priceCache`** — cached prices indefinitely. Added 5-min TTL. This also fixes a correctness bug — price changes weren't reflected until restart.

Not a DB connection leak at all. The `requestLog` array was the biggest offender — on a moderately busy API it would consume hundreds of MBs within days.
