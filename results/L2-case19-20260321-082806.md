All changes are consistent. Here's the summary:

### Fixed: `softDeleteOrder` now sets `deleted_at`
- `order-service.js:74` — sets `deleted_at: db.fn.now()` alongside `status: 'deleted'`
- This fixes the bug where `getUserOrders()` filtered by `whereNull('deleted_at')` but deleted orders never had it set

### Aligned: All soft-delete filtering now uses `whereNull('deleted_at')`
**Orders** — migrated 5 locations from `status !== 'deleted'` checks:
- `order-service.js:14,26,38` — list/search/count queries
- `routes/orders.js:32,67,82` — single order, stats, and delete endpoints

**Products** — migrated from `is_active` boolean:
- `db.js:38` — added `deleted_at` column to products schema
- `product-service.js:15` — `listProducts()` now uses `whereNull('deleted_at')`
- `product-service.js:19-24` — new `softDeleteProduct()` function (sets both `is_active: false` and `deleted_at` for backward compat)
- `routes/products.js:23` — single product check uses `deleted_at`

**Users** — already used `deleted_at`, no changes needed.

Note: `softDeleteProduct` still sets `is_active: false` alongside `deleted_at` so any existing code reading `is_active` directly won't break during transition.
