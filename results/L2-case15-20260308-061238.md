Fixed. `RATE_LIMIT` was hardcoded to `100` — never reading `process.env.RATE_LIMIT`. Now it reads the env var with `100` as fallback. Your `.env` value of `10` will take effect.

Also worth noting: `requestLog` in `rate-limiter.js:4` is an unbounded array that never gets pruned — it's a memory leak. The `hits` Map gets cleaned up by the interval, but `requestLog` grows forever. Not related to your issue, but flag it for later if you want.
