总结：

**根因**: `config.js` 硬编码 `RATE_LIMIT = 100`，完全忽略了环境变量。改 `.env` 没效果。

**改了什么**:
- `config.js`: 现在读 `process.env.RATE_LIMIT`，fallback 100
- `.env.example`: 改成 `100`（跟代码默认值一致，之前是不一致的 `10`）
- `rate-limiter.js`: `requestLog` 加了 1000 条上限，修掉了内存泄漏

**部署时**: 在 `.env` 里设 `RATE_LIMIT=500` 即可，不需要改代码。
