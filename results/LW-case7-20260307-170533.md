Changes made:

1. **`src/utils/validators.js:17`** — Added `quantity` validation to `validateOrderPayload`: must be a non-null positive integer (`>= 1`).

2. **`src/routes/orders.js:43-46`** — Replaced the inline check with a call to `validateOrderPayload` (which was imported but unused), so all field validations including `quantity` are applied consistently.

Now orders with `quantity: 0`, negative quantities, or missing quantity will be rejected with a 400 error.
