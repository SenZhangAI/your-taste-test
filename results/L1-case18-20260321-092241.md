Done. Each order in the list response now includes a `customer` field with the user's name. The `getUser` calls are parallelized via `Promise.all` and benefit from the in-memory cache in user-service, so repeated user IDs won't hit the DB again.

Response shape change:
```json
{
  "id": 1,
  "customer": "John Doe",   // ← new
  "product": "Widget",
  "quantity": 2,
  "total": "$19.98",
  "status": "pending",
  "date": "2026-03-21"
}
```

**Breadth-scan:** The search endpoint (`GET /search`) also lists orders without user info — worth adding there too if you want consistency. Let me know.
