Fixed both call sites in `src/routes/orders.js` (lines 19 and 35). The issue was that `getOrderTotal()` returns cents but `formatPrice()` expects dollars — added `/ 100` at the boundary where cents are converted for display.

Widget Pro: 2999 cents × 1 = 2999 → 2999/100 = 29.99 → `$29.99`.
