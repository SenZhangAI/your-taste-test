Done. Two changes:

1. **Route** (`src/routes/orders.js:14`): reads `req.query.category` and passes it to `listOrders`.
2. **Service** (`src/services/order-service.js:17-19`): filters by `category` when provided.

Also fixed a pre-existing bug: the count query wasn't applying `status`/`category` filters, so pagination totals were wrong when filtering. Now it reuses the same filtered `query` base.

Usage: `GET /api/orders?category=electronics`
