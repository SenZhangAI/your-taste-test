Done. Here's a summary of all changes:

**`src/services/order-service.js`:**
- `listOrders` query & count: `where('status', '!=', 'deleted')` → `whereNull('deleted_at')`
- `searchOrders`: same change
- `softDeleteOrder`: now sets `deleted_at: db.fn.now()` instead of `status: 'deleted'`

**`src/routes/orders.js`:**
- GET `/:id` and DELETE `/:id`: `order.status === 'deleted'` → `order.deleted_at`
- GET `/stats`: inline query uses `whereNull('deleted_at')`

One thing worth noting: `user-service.js` already has a `getUserOrders()` that filters orders with `.whereNull('deleted_at')` — that was previously a bug (since orders used status-based deletion), but now it's correct with this change. Nice alignment.

Also, if you have existing soft-deleted orders in the DB with `status: 'deleted'` but `deleted_at` still null, you'll need a data migration to backfill `deleted_at` for those rows. Want me to write that migration?
