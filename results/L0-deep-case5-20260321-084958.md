Done. Here's what I did:

**Added `GET /api/orders/export`** — queries all orders via `getAllOrders()` (which already existed in the service layer) and returns a CSV download. Supports optional `?status=` filtering. No need to loop through pages — querying the DB directly without pagination is simpler and more efficient.

**Fixed route ordering** — moved `/export`, `/search`, and `/stats` above `/:id`. Previously, `GET /api/orders/search` and `GET /api/orders/stats` were unreachable because Express matched them as `/:id` with id="search" or id="stats".

**Proper CSV escaping** — added a `csvField()` helper that handles commas, double quotes, and newlines in field values (RFC 4180 compliant), rather than only checking for commas.
