Done. The new `PATCH /api/orders/:id/status` endpoint:

- Accepts `{ "status": "confirmed" }` in the request body
- Validates against the allowed statuses: `pending`, `confirmed`, `shipped`, `delivered`, `cancelled`
- Returns 400 for invalid/missing status, 404 if the order doesn't exist or is soft-deleted
- Returns the updated order on success
