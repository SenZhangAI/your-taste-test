Now `getOrder` matches its JSDoc — it returns `null` for soft-deleted orders. The existing `order.status === 'deleted'` checks in the route handlers are now redundant but harmless (defense-in-depth), so I'm leaving them in place.
