The never-expiring `userCache` Map was serving stale user data. Removed it entirely — `getUser` now always hits the DB, so the `deleted_at` check in the route works correctly.

**Breadth-scan — related issues I noticed:**

1. **Orders for deleted users still visible** in `listOrders` and `searchOrders` — these filter by `order.status != 'deleted'` but don't check whether the **user** is deleted. Depending on your business rules, you may want to filter those out too (or join on users to exclude them). This is a separate decision though — deleted user's orders might still be valid for accounting.

2. **Orders route path conflict**: `/search` and `/stats` are defined *after* `/:id`, so `GET /orders/search` and `GET /orders/stats` will match the `/:id` route first. They should be moved above it.

Want me to address either of those?
