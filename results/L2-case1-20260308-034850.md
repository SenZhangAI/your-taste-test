Done. The `GET /api/orders?category=electronics` filter is wired up:

- **Route** (`src/routes/orders.js:12-13`): extracts `category` from query params and passes it to the service
- **Service** (`src/services/order-service.js:10-17`): applies a `.where('category', category)` filter to both the data query and the count query when the parameter is present. Uses `.clone()` to share the base query between both.

The `category` column already has an index from the migration, so filtering will be efficient.
