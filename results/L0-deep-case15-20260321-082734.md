Fixed. The value was hardcoded to `100`, ignoring the env var entirely. Now it reads `process.env.RATE_LIMIT` (wrapped in `Number()` since env vars are strings), falling back to `100` if unset. With `RATE_LIMIT=10` in your `.env`, the limiter will now enforce 10 requests per window.
