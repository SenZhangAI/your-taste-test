Changes made:

1. **`src/utils/validators.js:16`** — `validateOrderPayload` now checks `quantity` is a positive integer (rejects `0`, negatives, `null`, non-integers).

2. **`src/routes/orders.js:43-46`** — Replaced the inline validation with `validateOrderPayload`, which was already imported but unused. This gives consistent, centralized validation including the new quantity check.
