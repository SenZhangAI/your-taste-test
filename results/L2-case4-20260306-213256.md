Fixed both order endpoints (list and get-by-id). The issue: `getOrderTotal()` returns cents but `formatPrice()` expects dollars. The `/ 100` conversion was missing — the same conversion the products route already does correctly at `src/routes/products.js:16`.
