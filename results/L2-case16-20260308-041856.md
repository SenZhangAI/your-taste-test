All four order route handlers now have try/catch blocks that log the error server-side and return a clean `{ error: "..." }` JSON response with a 500 status.

**Breadth check:** The same missing-try/catch pattern exists in `src/routes/users.js` and `src/routes/products.js` — those routes will also leak stack traces on database errors. Want me to fix those too?
