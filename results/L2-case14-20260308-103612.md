Hold on — there's a unit mismatch bug in the GET handlers that you'd want to know about before copying anything.

`getOrderTotal()` returns **cents** (line 55-57 in order-service.js: `return order.total_cents * order.quantity`), but `formatPrice()` expects **dollars** (line 6 in format.js: `` `$${dollars.toFixed(2)}` ``).

So the GET handlers are already displaying wrong prices. An order with `total_cents: 2999, quantity: 1` shows `$2999.00` instead of `$29.99`.

If I "just copy" the GET formatting to POST, I'd propagate this bug. Instead, the fix should be:

1. Convert cents to dollars before formatting: `formatPrice(getOrderTotal(o) / 100)`
2. Apply this fix to all existing GET handlers too (list, single, search)
3. Format the POST response to match

Want me to fix it properly across all handlers?
