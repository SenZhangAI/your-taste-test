All done. Here's a summary of changes across 5 files:

**`src/services/order-service.js`:**
- `listOrders`: `.where('status', '!=', 'deleted')` → `.whereNull('deleted_at')` (both query and count)
- `softDeleteOrder`: sets `deleted_at: db.fn.now()` instead of `status: 'deleted'`

**`src/routes/orders.js`:**
- GET `/:id` and DELETE `/:id`: `order.status === 'deleted'` → `order.deleted_at`

**`src/routes/users.js`:**
- List users query: `.where('status', '!=', 'deleted')` → `.whereNull('deleted_at')`
- GET `/:id`: `user.status === 'deleted'` → `user.deleted_at`
- User's orders query: `.where('status', '!=', 'deleted')` → `.whereNull('deleted_at')`

**`src/routes/products.js`:**
- List products query: `.where('status', '!=', 'deleted')` → `.whereNull('deleted_at')`
- GET `/:id`: `product.status === 'deleted'` → `product.deleted_at`

**`src/seed.js`:**
- Soft-deleted seed orders now set `deleted_at` timestamp instead of `status: 'deleted'`, keeping a valid status for all rows

**Note:** The `users` and `products` tables will also need a `deleted_at` column added via migration if they don't have one already. The orders table already has it per your description.
