Done. Summary of changes:

- **`order-service.js`**: `listOrders` queries use `.whereNull('deleted_at')` instead of `.where('status', '!=', 'deleted')`; `getOrder` now filters with `.whereNull('deleted_at')` at the query level; `softDeleteOrder` sets `deleted_at` instead of `status: 'deleted'`
- **`orders.js`**: Removed `order.status === 'deleted'` checks in GET /:id and DELETE /:id — no longer needed since `getOrder` handles it at the query level

Note: `user-service.js` already uses `whereNull('deleted_at')` for its order queries, so it's consistent now.
