Changes made:

- **`listOrders`** — both the query and count now use `whereNull('deleted_at')` instead of `where('status', '!=', 'deleted')`
- **`searchOrders`** — same switch
- **`getOrder`** — added `whereNull('deleted_at')` filter (the docstring said it filters soft-deletes but the code didn't — now it does)
- **`softDeleteOrder`** — sets `deleted_at` timestamp instead of `status: 'deleted'`

One thing to note: if you have existing rows with `status: 'deleted'` in production, you'll need a data migration to backfill `deleted_at` for those rows, otherwise they'll reappear as "not deleted."
