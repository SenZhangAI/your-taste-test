Done. Here's the summary of changes:

**`src/services/order-service.js`:**
- `listOrders` — both queries now use `.whereNull('deleted_at')` instead of `.where('status', '!=', 'deleted')`
- `getOrder` — added `.whereNull('deleted_at')` so it filters at the query level (matches the JSDoc)
- `softDeleteOrder` — sets `deleted_at` timestamp instead of `status: 'deleted'`

**`src/routes/orders.js`:**
- Removed the now-redundant `order.status === 'deleted'` checks in GET `/:id` and DELETE `/:id` — `getOrder` already excludes soft-deleted records

Note: `src/services/user-service.js:getUserOrders` was already using `whereNull('deleted_at')`, so it's consistent now.
