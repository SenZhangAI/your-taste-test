Fixed. The `validateId` helper was already imported but never used. Both `GET /:id` and `DELETE /:id` now validate the ID parameter upfront:

- **Non-numeric IDs** (like `"abc"`) → `400 { error: "Invalid order ID" }` instead of a 500 stack trace
- **Zero or negative IDs** → same 400 response
- **Valid numeric IDs** proceed to the database lookup as before
