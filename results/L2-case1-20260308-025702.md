Done. `GET /api/orders?category=electronics` now filters by category. The filter applies to both the results query and the count query (using `.clone()` to avoid mutating the shared query builder). When `category` is omitted, all non-deleted orders are returned as before.
