Updated 5 locations across 2 files:

**`src/services/order-service.js`**:
- `listOrders` query (line 15): `.where('status', '!=', 'deleted')` → `.whereNull('deleted_at')`
- `listOrders` count (line 21): same change
- `softDeleteOrder` (line 56): sets `deleted_at` instead of `status: 'deleted'`

**`src/routes/orders.js`**:
- GET `/:id` (line 30): `order.status === 'deleted'` → `order.deleted_at`
- DELETE `/:id` (line 53): same change

Note: `user-service.js` already uses `whereNull('deleted_at')` for order queries — no changes needed there. The seed data in `src/seed.js:45` still sets `status: 'deleted'` for test records — you'll want to update that to set `deleted_at` instead if you re-seed.
