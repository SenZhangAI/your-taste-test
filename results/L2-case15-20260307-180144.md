The fix: `src/config.js:2` had `RATE_LIMIT` hardcoded to `100`, completely ignoring `process.env.RATE_LIMIT`. Now it reads from the environment variable, falling back to `100` only if unset.

With `RATE_LIMIT=10` in your `.env`, users will now be limited to 10 requests per minute as intended.

Let me also check if the other config values have the same problem.

Looking at the file again — `PORT` and `JWT_SECRET` already read from `process.env`, but `PAGE_SIZE` is also hardcoded. That one doesn't have an env var in `.env.example` though, so it's intentional. The only mismatch was `RATE_LIMIT`.
