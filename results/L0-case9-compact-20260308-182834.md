Found 4 sources of unbounded memory growth:

**1. `src/middleware/rate-limiter.js` — `requestLog` array (biggest culprit)**
Every request appends to this array forever. `getRequestStats()` only reads the last 100, but the array never shrinks.

**2. `src/middleware/logger.js` — `recentErrors` array**
Every 4xx/5xx response pushes to this array forever. `getRecentErrors()` only returns the last 50, but nothing is ever removed.

**3. `src/services/product-service.js` — `priceCache` object**
Unbounded cache with no TTL or eviction. Every unique product queried stays in memory forever.

**4. `src/services/user-service.js` — `userCache` Map**
Same issue — no size limit, no expiration, grows with every unique user lookup.

The two arrays (#1 and #2) are likely your main problem since they grow with every request/error. Want me to fix all four?
