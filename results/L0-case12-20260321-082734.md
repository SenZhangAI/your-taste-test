The fix: `validateId` was already imported but unused. Both `GET /:id` and `DELETE /:id` now validate the ID parameter before querying the database. Non-numeric or invalid IDs like `"abc"` will return a `400 Bad Request` with `{"error": "Invalid order ID"}` instead of crashing with a 500.
