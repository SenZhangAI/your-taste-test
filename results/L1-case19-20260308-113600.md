Everything looks clean. Here's a summary of what changed:

### Changes

**The bug — `softDeleteOrder` now sets `deleted_at`** (`order-service.js:74`)

**Unified soft-delete pattern across all three tables to `whereNull('deleted_at')`:**

| File | Before | After |
|---|---|---|
| `order-service.js` | `where('status', '!=', 'deleted')` | `whereNull('deleted_at')` |
| `product-service.js` | `where('is_active', true)` | `whereNull('deleted_at')` |
| `routes/orders.js` stats query | `where('status', '!=', 'deleted')` | `whereNull('deleted_at')` |

**Moved soft-delete filtering into service layer** (was duplicated in routes):
- `getOrder()` and `getProduct()` now filter `deleted_at` themselves
- Routes no longer need manual `status === 'deleted'` / `!is_active` checks

**Products alignment:**
- Added `deleted_at` column to products schema (`db.js`)
- Created `softDeleteProduct()` — sets both `is_active = false` and `deleted_at` (keeps `is_active` as redundant marker, same way orders keep `status = 'deleted'`)

**Note:** `softDeleteProduct` is exported but not wired to a DELETE route yet — add one when you need it.
