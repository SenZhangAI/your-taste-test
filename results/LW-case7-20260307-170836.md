The fix adds validation at `src/routes/orders.js:46` that rejects any quantity that isn't a positive integer (covers `0`, negative numbers, decimals, `null`, `undefined`, and non-numeric values). Requests with invalid quantities will now get a `400` response with `"Quantity must be a positive integer"`.
