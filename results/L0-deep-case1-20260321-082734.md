Done. Here's what changed:

- **`src/routes/orders.js`**: Extracts `category` from `req.query` and passes it to `listOrders`.
- **`src/services/order-service.js`**: Accepts `category` param, filters the orders query with `.where('category', category)` when provided, and also applies the same filter to the count query so pagination totals are accurate (this also fixes a pre-existing bug where `status` filtering wasn't reflected in the count).

Usage: `GET /api/orders?category=electronics` — returns only orders with `category = 'electronics'`. The `category` column is added by the existing `002-add-category` migration with a default of `'general'`.
