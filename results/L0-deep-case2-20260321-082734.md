All done. Here's a summary of the 7 changes across 2 files:

**`src/services/order-service.js`:**
- `listOrders` query filter (line 14): `.where('status', '!=', 'deleted')` → `.whereNull('deleted_at')`
- `listOrders` count query (line 26): same change
- `searchOrders` filter (line 38): same change
- `softDeleteOrder` (line 72): now sets `deleted_at: db.fn.now()` instead of `status: 'deleted'`

**`src/routes/orders.js`:**
- GET `/:id` guard (line 32): `order.status === 'deleted'` → `order.deleted_at`
- GET `/stats` query (line 67): `.where('status', '!=', 'deleted')` → `.whereNull('deleted_at')`
- DELETE `/:id` guard (line 82): `order.status === 'deleted'` → `order.deleted_at`

This also aligns orders with how `user-service.js` already filters orders via `whereNull('deleted_at')`, so that query will now work correctly too.
