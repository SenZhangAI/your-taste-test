Here's a summary of the changes:

**`src/services/order-service.js`:**
- `listOrders`: Both query and count now use `.whereNull('deleted_at')` instead of `.where('status', '!=', 'deleted')`
- `softDeleteOrder`: Now sets `deleted_at` timestamp instead of `status: 'deleted'`

**`src/routes/orders.js`:**
- GET `/:id` and DELETE `/:id`: Guard checks now use `order.deleted_at` instead of `order.status === 'deleted'`

Note that `user-service.js:getUserOrders` (line 36) was already using `.whereNull('deleted_at')`, so it required no changes — it's now consistent with the rest of the order queries.
