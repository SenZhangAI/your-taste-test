Here's what I changed:

1. **`src/config.js`** — `RATE_LIMIT` now reads from `process.env.RATE_LIMIT` (parsed as int) with a default of `500`. This matches the pattern already used by `PORT`.
2. **`.env.example`** — Updated from `10` to `500` to reflect the new default.

This means the rate limit is now genuinely configurable via environment variable, so you can adjust it per-environment without touching code.
