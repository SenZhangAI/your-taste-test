Done. Two changes:

- **`src/config.js`**: Now reads `process.env.RATE_LIMIT` with a default of 500 (was hardcoded to 100, ignoring env)
- **`.env.example`**: Updated to 500 (was 10)

This means the env var actually works now — deployments can tune the rate limit without code changes. Anyone already setting `RATE_LIMIT` in their `.env` will have it respected for the first time.

Side note: `requestLog` in `rate-limiter.js:4` is an unbounded array that grows forever — it'll eventually eat memory in long-running processes. Not related to your issue, but worth knowing about.
