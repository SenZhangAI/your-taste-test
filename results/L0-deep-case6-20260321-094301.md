The root cause: `getOrder` at `src/services/order-service.js:48` was missing the `.where('status', '!=', 'deleted')` filter that every other query function (`listOrders`, `searchOrders`) already has. The JSDoc was correct about the *intended* behavior, but the implementation didn't match.

The route handler's `order.status === 'deleted'` check on `src/routes/orders.js:32` was acting as an accidental safety net for the GET endpoint, but `getOrder` is also called from `createOrder` (line 68) and the DELETE handler (line 81) — anywhere trusting the JSDoc would get wrong results.
