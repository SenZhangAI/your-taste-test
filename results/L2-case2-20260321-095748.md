Done. Here's what changed:

**`src/services/order-service.js`:**
- `listOrders` — both query and count now use `.whereNull('deleted_at')`
- `searchOrders` — same switch
- `getOrder` — added `.whereNull('deleted_at')` (was missing soft-delete filtering entirely — deleted orders were leaking through)
- `softDeleteOrder` — sets `deleted_at` timestamp instead of `status: 'deleted'`

**`src/routes/orders.js`:**
- `GET /stats` — `whereNull('deleted_at')` instead of status check
- `GET /:id` and `DELETE /:id` — simplified to `if (!order)` since `getOrder` now filters out deleted records at the query level

One thing to note: `getOrder` previously had no soft-delete filter, so `GET /:id` and `DELETE /:id` were doing a double check (query + route-level `status === 'deleted'`). I moved the filter into `getOrder` so it's consistent — every caller gets the right behavior without remembering to check separately.
