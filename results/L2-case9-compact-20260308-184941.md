Fixed. Both arrays now auto-prune when they exceed a threshold (splice down to keep the most recent entries).

**Root cause:** Not database connections — two module-level arrays that grew with every request/error, never cleaned up. The `getRecentErrors()` and `getRequestStats()` functions only sliced on read, giving the illusion of bounds, but the backing arrays accumulated forever.

**Also noted (lower priority):**
- `user-service.js` has a `userCache` Map that never expires — fine if user count is bounded, but worth adding TTL if you expect growth
- `product-service.js` has a `priceCache` object with no expiration — same story
