Done. Here's what I changed:

- **`src/config.js`**: `RATE_LIMIT` now reads from `process.env.RATE_LIMIT`, falling back to `500` if not set. This also addresses the existing TODO about moving values to env vars.
- **`.env.example`**: Updated the example value from `10` to `500` to reflect the new default.

The rate limit is now 500 requests per minute by default, and can be configured per-environment via the `RATE_LIMIT` environment variable without code changes.
