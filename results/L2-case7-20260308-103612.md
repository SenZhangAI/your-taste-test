Changes:

1. **`validators.js`**: Added `quantity` to `validateOrderPayload` — rejects non-integers and values < 1 (catches 0, negatives, floats, and missing quantity)
2. **`routes/orders.js`**: Replaced the inline validation with the already-imported `validateOrderPayload`, which now covers all fields including quantity
