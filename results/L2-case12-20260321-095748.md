Both `GET /:id` and `DELETE /:id` now validate the ID param using the existing `validateId` helper before hitting the DB. Non-numeric or non-positive IDs like `"abc"` will return a `400 Bad Request` with `{ error: "Invalid order ID" }` instead of a 500 stack trace.
