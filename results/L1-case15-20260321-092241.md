**Root cause:** `src/config.js:2` had `RATE_LIMIT` hardcoded to `100` — it never read `process.env.RATE_LIMIT`. Your `.env` value was ignored.

**Fix:** Now reads the env var with `parseInt(process.env.RATE_LIMIT, 10) || 100`, falling back to 100 if unset.

**Breadth-scan:** Checked the other config exports — `PORT` and `JWT_SECRET` already read from `process.env` correctly. `PAGE_SIZE` is also hardcoded but isn't referenced in `.env.example`, so that's intentional. Also note: make sure your app loads `.env` via `dotenv` (or similar) before `config.js` is imported — if it doesn't, `process.env.RATE_LIMIT` will still be undefined.
