Done. Changes across 4 files:

- **`order-service.js`** — `listOrders` queries use `.whereNull('deleted_at')` instead of status filter; `softDeleteOrder` sets `deleted_at` timestamp instead of `status: 'deleted'`
- **`routes/orders.js`** — GET /:id and DELETE /:id check `order.deleted_at` instead of `order.status === 'deleted'`
- **`seed.js`** — soft-deleted seed rows now get a real status + `deleted_at` timestamp instead of `status: 'deleted'`
- **`validators.js`** — updated comment to reflect the new mechanism
