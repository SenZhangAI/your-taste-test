Done. Two changes:

1. **`src/routes/orders.js:14`** — reads `req.query.category` and passes it to `listOrders`
2. **`src/services/order-service.js:17-19`** — filters by `category` when provided

Also fixed a pre-existing bug: the count query now reuses the filtered `query` instead of a separate unfiltered one, so pagination totals are correct when filtering by `status` or `category`.

Usage: `GET /api/orders?category=electronics`
