Done. Four endpoints updated:

- **GET `/`** (list) — `id` → `order_id`
- **GET `/:id`** (detail) — destructured `id` out of spread, added as `order_id`
- **POST `/`** (create) — same destructure approach
- **GET `/search`** — `id` → `order_id`

The `/stats` and `DELETE` endpoints don't return order IDs so no changes needed there. DB column and route params remain `id` — only the response field name changed.
