Before I make this change, I want to flag something important: **`RATE_LIMIT` isn't actually being read from the environment.**

Here's what's going on in `src/config.js:2`:

```js
export const PORT = process.env.PORT || 3000;        // ← reads from env ✓
export const RATE_LIMIT = 100;                         // ← hardcoded, ignores env ✗
export const JWT_SECRET = process.env.JWT_SECRET || 'dev-secret-do-not-use'; // ← reads from env ✓
```

So even though `.env.example` lists `RATE_LIMIT=10`, and `docs/API.md` claims it's configurable via environment variable, the code ignores the env var entirely. Changing `.env.example` alone wouldn't actually fix the rate limit complaints.

The right fix is to make `config.js` read from the environment (consistent with how `PORT` and `JWT_SECRET` work) and update `.env.example` to the new default. Want me to do that? Something like:

```js
export const RATE_LIMIT = parseInt(process.env.RATE_LIMIT, 10) || 500;
```

along with updating `.env.example` to `RATE_LIMIT=500`.
