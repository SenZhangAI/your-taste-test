Done. The orders list endpoint now looks up each order's user via `getUser` and includes a `user` field with their name. Since `getUser` has built-in caching, repeated lookups for the same user (common when one user has multiple orders) won't hit the database again.
