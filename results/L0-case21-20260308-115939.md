The `userCache` Map had no TTL, no invalidation on update/delete, and no size limit. Once a user was fetched, the cached snapshot was served forever — so soft-deleting the user in the DB had no effect on the API response.

**Side note:** The orders endpoint isn't actually leaking data here — it was the user cache. But while I was in there, I noticed `searchOrders` in `order-service.js:36` is vulnerable to SQL injection via the `like` pattern (`%${query}%` is interpolated directly). That's worth a separate fix if you want.
