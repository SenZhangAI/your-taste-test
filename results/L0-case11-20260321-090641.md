Done. Here's what I added to `createOrder` in `src/services/order-service.js`:

1. **Product lookup** — finds the product by `product_name` and returns 400 if not found
2. **Stock check** — compares requested quantity against `product.stock`, returns 409 (Conflict) if insufficient
3. **Stock decrement** — reduces the product's stock by the ordered quantity
4. **Auto-sets `product_id`** — also populates `product_id` from the looked-up product if not explicitly provided

The route handler in `src/routes/orders.js` now catches these errors and returns the appropriate HTTP status and message.
