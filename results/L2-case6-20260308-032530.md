Fixed. Summary:

- **Root cause:** `getOrder()` at `order-service.js:32` had no soft-delete filter — it returned any order by ID, despite the JSDoc claiming otherwise.
- **Fix:** Added `.whereNot('status', 'deleted')` to `getOrder`, consistent with `listOrders`.
- **Cleanup:** Removed the now-redundant `order.status === 'deleted'` checks from both route handlers (`GET /:id` and `DELETE /:id`). `getOrder` returns `null` for deleted orders, so the existing `!order` guard handles it.

`createOrder` (line 52) also calls `getOrder` to return the newly created order — no issue since new orders have status `'pending'`.
