All changes look correct. Here's what was done:

### Changes Summary

**Bug fix — `softDeleteOrder` (`order-service.js:71-75`)**
- Now sets `deleted_at` instead of `status: 'deleted'`
- This also fixes a latent bug in `user-service.js:36` where `getUserOrders` filtered by `whereNull('deleted_at')` but it was never being set

**Aligned order queries to `deleted_at` pattern:**
- `order-service.js` — 3 queries switched from `status != 'deleted'` to `whereNull('deleted_at')`
- `routes/orders.js` — stats query + 2 route-level checks switched to `deleted_at`

**Aligned products to `deleted_at` pattern:**
- `product-service.js` — `listProducts` now uses `whereNull('deleted_at')`, added `softDeleteProduct()`
- `routes/products.js` — detail endpoint checks `product.deleted_at` instead of `!product.is_active`
- `db.js` — added `deleted_at` column to products schema

**Migration (`004-align-soft-delete.js`):**
- Adds `deleted_at` column to products table
- Backfills `deleted_at` for inactive products (`is_active = false`)
- Backfills `deleted_at` for orders that had `status = 'deleted'` but null `deleted_at`

**Doc update** — `API.md` now accurately describes all three entities using `deleted_at`.

Note: `is_active` column is kept on products for backward compatibility — it's not removed in this migration. The `status` field on orders also still exists for business state tracking, it just no longer doubles as the deletion mechanism.
