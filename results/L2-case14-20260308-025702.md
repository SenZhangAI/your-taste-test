Done. The POST response now applies `formatPrice` and `formatDate` the same way GET /:id does, so `total` will be `"$29.99"` and `date` will be `"2026-03-01"` format.

Note: the raw `total_cents` and `created_at` fields will still be present via the spread (`...order`), matching the GET /:id behavior. If you'd prefer to strip those raw fields from the response (like GET `/` does with its explicit pick list), let me know.
