Two changes made:

1. **`src/utils/validators.js:16`** — Added quantity validation to `validateOrderPayload`: rejects `null`/`undefined`, non-integers, and values ≤ 0 (`quantity must be a positive integer`).

2. **`src/routes/orders.js:43-49`** — Replaced the inline field checks with a call to `validateOrderPayload` (which was already imported but unused), so all validation logic lives in one place.

Now a request with `quantity: 0` (or negative, non-integer, or missing) will get a `400` with a descriptive error message instead of creating a `$0.00` order.
