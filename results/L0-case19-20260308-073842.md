That's the fix — one line added at `order-service.js:58`.

Regarding aligning products: I'd hold off on that. Products use an `is_active` boolean flag (`product-service.js:15`), and the products table doesn't even have a `deleted_at` column. That's a fundamentally different soft-delete pattern — migrating it would mean a schema change, updating all product queries, and doesn't solve any immediate bug. Better to keep that as a separate effort if you decide to standardize later.
