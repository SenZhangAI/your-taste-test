Here's what I did:

1. **POST `/api/orders`** — added `total` and `date` formatting to the response, matching GET.
2. **Fixed cents→dollars bug across all handlers** — `getOrderTotal` returns cents (per its JSDoc: `@returns {number} Total in cents`), but `formatPrice` expects dollars (per its JSDoc: `@param {number} dollars`). All callers were passing cents directly, so `total_cents: 2999` would render as `$2999.00` instead of `$29.99`. Added `/ 100` at every call site.

The contracts on both functions are clear and correct — the callers just weren't doing the conversion.
