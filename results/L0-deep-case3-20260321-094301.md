Here's what I changed:

1. **`src/config.js`** — `RATE_LIMIT` now reads from `process.env.RATE_LIMIT` (with `500` as the default), matching how `PORT` and `JWT_SECRET` already work.
2. **`.env.example`** — Updated from `10` to `500` to reflect the new default.

This means the rate limit is now actually configurable via the environment, so you (or ops) can tune it without code changes in the future.
