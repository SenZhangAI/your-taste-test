Two changes made:

1. **`src/utils/validators.js:16`** — Added `quantity` validation to `validateOrderPayload`: rejects `null`/`undefined`, non-integers, and values `<= 0`.

2. **`src/routes/orders.js:43-50`** — Replaced the ad-hoc field checks with the existing `validateOrderPayload` function (which was imported but unused), so all validation is centralized and quantity is now properly checked.

Orders with quantity `0`, negative quantities, or non-integer quantities will now get a `400` response with `"quantity must be a positive integer"`.
