Done. Two changes:

1. **`src/services/order-service.js:14-21`** — `listOrders` now joins the `users` table to select `users.name as user_name` alongside order data, avoiding N+1 queries.
2. **`src/routes/orders.js:19`** — Added `user: o.user_name` to the list response.

Each order in the response now includes a `user` field with the user's name.
