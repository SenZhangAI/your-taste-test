**Breadth-scan:** The `listOrders` in order-service.js already uses proper SQL with `COUNT` and pagination — no issue there. The `searchOrders` uses `LIKE` with a leading wildcard which won't use indexes well at scale, but that's a separate concern and you didn't report it as slow.

**Summary of changes:**

1. **`src/routes/orders.js` — stats endpoint rewritten** to use `COUNT(*)`, `SUM()`, and `GROUP BY` in SQL instead of loading all rows into memory. This goes from O(n) memory + data transfer to a single index scan returning ~5 rows.

2. **New migration `004-add-orders-status-index.js`** — adds an index on `orders.status`. This speeds up the `WHERE status != 'deleted'` filter used by stats, list, and count queries.

Expected improvement: 30s → <100ms. The bottleneck was never the DB query speed — it was fetching 500k rows over the wire and iterating in JS.
