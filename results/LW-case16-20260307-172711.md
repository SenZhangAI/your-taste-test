All four route handlers (`GET /`, `GET /:id`, `POST /`, `DELETE /:id`) now have try/catch blocks that:

1. Log the full error to the server console for debugging (`console.error`)
2. Return a clean `500` JSON response (`{ error: "Failed to ..." }`) without leaking stack traces or server internals to the client
