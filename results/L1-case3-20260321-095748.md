Done. Changes:

- **`src/config.js`** — now reads `process.env.RATE_LIMIT` with a default of 500 (was hardcoded to 100, ignoring env)
- **`.env.example`** — updated from 10 to 500 to match the new default

This means existing deployments that set `RATE_LIMIT` in their actual `.env` will use that value; those that don't will get 500 instead of the old hardcoded 100.

Separate concern: that `requestLog` array in `rate-limiter.js` will grow unbounded and eventually eat all memory. Worth addressing separately if you're seeing memory issues too.
