Fixed. `RATE_LIMIT` now reads from `process.env.RATE_LIMIT` with `parseInt` (since env vars are strings) and falls back to `100` if unset. Your `.env` setting of `RATE_LIMIT=10` will now be respected.
