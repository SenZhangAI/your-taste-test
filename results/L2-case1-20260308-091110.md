Done. Two changes:

1. **`src/routes/orders.js:14`** — reads `req.query.category` and passes it to `listOrders`.
2. **`src/services/order-service.js:10`** — accepts `category`, adds a `.where('category', category)` clause when present.

Also fixed a pre-existing bug: the pagination `count` query was ignoring `status` (and would have ignored `category`). It now reuses the filtered query for the count so `total` reflects the actual filtered result set.

Usage: `GET /api/orders?category=electronics` (combinable with existing `status`, `sort`, `page`, `limit` params).
