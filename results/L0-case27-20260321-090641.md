Done. Here's what I changed:

**`src/routes/orders.js`** — 4 endpoints updated:
- **GET /api/orders** (list): `id: o.id` → `order_id: o.id`
- **GET /api/orders/:id** (single): destructure `id` from the spread and output as `order_id`
- **POST /api/orders** (create): destructure `id` from the created order and output as `order_id`
- **GET /api/orders/search**: `id: o.id` → `order_id: o.id`

**`src/routes/users.js`** — 1 endpoint updated:
- **GET /api/users/:id**: maps the nested orders array to rename `id` → `order_id`

The database column, route parameters (`:id`), internal service calls, and non-order APIs (users, products) are all left unchanged — only the JSON response field name was renamed.
