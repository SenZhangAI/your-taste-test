Not a caching issue — the `getOrder` function at `src/services/order-service.js:48` simply wasn't filtering out soft-deleted records. The JSDoc claimed it did, but the query was just `.where({ id }).first()` with no status check.

The other functions (`listOrders`, `searchOrders`) all correctly filter with `.where('status', '!=', 'deleted')`, so this was just missed on `getOrder`. Fixed by adding the same filter. Order #42 should now return `null` from the API as expected.
