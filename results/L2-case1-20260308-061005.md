Done. `GET /api/orders?category=electronics` now filters by category. The shared query builder ensures both the results and the count reflect the same filter. If no `category` param is provided, behavior is unchanged.
