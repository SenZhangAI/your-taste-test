**This isn't a bug — price sorting was never implemented.** It's a missing feature.

Here's what's actually happening:

- `SORTABLE_FIELDS` at `src/services/order-service.js:4` only contains `['created_at', 'updated_at']`
- When you pass `sort=price`, line 12 checks `SORTABLE_FIELDS.includes('price')` → `false`, and silently falls back to `created_at`
- So the API isn't sorting alphabetically by price — it's not sorting by price at all. It's sorting by creation date.
- The API docs (`docs/API.md:24`) incorrectly list `price` as a supported sort field

Additionally, unknown sort fields fail silently (no error or warning), which makes it look like sorting is working when it isn't.

**Would you like me to add price sorting?** I'd add `total_cents` to the sortable fields (mapping the user-facing name `price` to the database column `total_cents`), so `$9.99` correctly sorts before `$99.99` using numeric comparison. I could also make unknown sort fields return an error instead of silently falling back.
