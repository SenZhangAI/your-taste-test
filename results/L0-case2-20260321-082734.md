All done. Here's what changed in `src/services/order-service.js`:

- **`listOrders`** — both the query filter (line 14) and the count query (line 26) now use `.whereNull('deleted_at')` instead of `.where('status', '!=', 'deleted')`
- **`searchOrders`** — same swap (line 38)
- **`softDeleteOrder`** — now sets `deleted_at: db.fn.now()` instead of `status: 'deleted'`

One thing to flag: `getOrder(id)` (line 48) doesn't filter out soft-deleted rows at all — it returns any order by ID regardless. You may want to add `.whereNull('deleted_at')` there too, depending on whether callers need to access deleted orders by ID.
