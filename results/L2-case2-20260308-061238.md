Done. Here's what changed:

- **`order-service.js`**: `listOrders` and `getOrder` now filter with `.whereNull('deleted_at')` instead of `.where('status', '!=', 'deleted')`. `softDeleteOrder` sets `deleted_at` timestamp instead of `status: 'deleted'`.
- **`routes/orders.js`**: Removed redundant `order.status === 'deleted'` checks — `getOrder` now handles this at the service layer.
- **`seed.js`**: Soft-deleted seed orders use `deleted_at` timestamp + a real status instead of `status: 'deleted'`.
- **`validators.js`**: Updated comment to reflect the new mechanism.

`user-service.js` has no status-based soft-delete patterns, so no changes needed there.
