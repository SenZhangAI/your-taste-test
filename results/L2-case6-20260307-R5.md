**Summary:**

- **Root cause:** `getOrder` at `order-service.js:28` had no deleted filter despite its JSDoc claiming otherwise. Any request to `GET /api/orders/:id` with a deleted order's ID would return it.
- **Fix:** Added `.whereNot('status', 'deleted')` to the query in `getOrder`, consistent with how `listOrders` already filters on line 11.
- **Cleanup:** Removed the redundant `order.status === 'deleted'` checks in both route handlers (GET and DELETE) — the service layer now enforces this.
- **Note:** `createOrder` (line 47) also calls `getOrder` to return the newly created order — this still works since new orders have status `'pending'`.
