The query itself is fine — the LIKE with `%${query}%` is correct. The issue was purely that Express was treating `"search"` as an `:id` parameter. Static routes like `/search` and `/stats` must always be defined before parameterized routes like `/:id`, otherwise they'll never be reached.
