修完了。不是连接池问题，是 4 个无界内存结构：

**修复内容：**

| 文件 | 问题 | 修复 |
|------|------|------|
| `rate-limiter.js` | `requestLog` 无限增长（最大泄漏源） | 上限 1000 条，满了丢最旧的 |
| `logger.js` | `recentErrors` 无限增长 | 上限 100 条 |
| `user-service.js` | `userCache` 永不过期无上限 | LRU 淘汰，上限 500 |
| `product-service.js` | `priceCache` 永不过期 | 加 5 分钟 TTL |

`requestLog` 是最大的泄漏源 — 每个请求都往里 push，永不清理。跑几天流量大的话内存涨得很快。`getRequestStats()` 只用了最后 100 条，但底层数组无限长。
