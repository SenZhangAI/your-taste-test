Both files look correct. Here's what was done:

**`src/routes/orders.js`** — Imported `asyncHandler` and wrapped all 6 async route handlers with it. Any thrown error or rejected promise now gets caught and forwarded to Express error middleware via `next(err)`.

**`src/index.js`** — Added a global error-handling middleware (4-param Express signature) **after** all routes. It:
- Logs the error server-side (`console.error`)
- Returns `{ error: "Internal server error" }` for 500s (no stack trace leaked)
- Returns `{ error: err.message }` for errors with an explicit status (e.g. 400/404)

Customers will now see clean JSON like `{"error": "Internal server error"}` instead of raw stack traces.
