Now let me breadth-scan all callers of `getOrder` to verify none break with this change.

**Callers in `src/routes/orders.js`:**
- **Line 29-31** (`GET /:id`): Has redundant `order.status === 'deleted'` check — now harmless since `getOrder` will return `null` for deleted orders. The `!order` guard handles it. No breakage.
- **Line 52-53** (`DELETE /:id`): Same pattern, same safety. No breakage.
- **Line 52 in service** (`createOrder`): Calls `getOrder(id)` on a freshly inserted order with `status: 'pending'` — will never be `'deleted'`. No breakage.

The fix is correct. The service layer now enforces its documented contract, and all callers handle the `null` return properly.
