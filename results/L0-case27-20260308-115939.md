Done. Here's a summary of the changes:

**`src/routes/orders.js`** — 4 endpoints updated:
- **GET `/`** (list): `id: o.id` → `order_id: o.id`
- **GET `/:id`** (detail): destructure `id` from the spread and emit as `order_id`
- **POST `/`** (create): destructure `id` from the returned order and emit as `order_id`
- **GET `/search`**: `id: o.id` → `order_id: o.id`

**`src/routes/users.js`** — 1 endpoint updated:
- **GET `/:id`** (user detail): map the nested orders array to rename `id` → `order_id`

The DELETE endpoint and stats endpoint were left unchanged since they don't return order `id` fields. The database layer and service layer are untouched — the rename is purely in the API response.
