Fixed. Both `GET /:id` and `DELETE /:id` now validate the ID parameter using the already-imported `validateId` helper before hitting the DB. Non-numeric or non-positive IDs return `400 { error: "Invalid order ID" }` instead of crashing with a 500.
