Done. `GET /api/orders?category=electronics` now filters by the `category` column. Changes:

- **`src/services/order-service.js:10`** — `listOrders` accepts a `category` param, applies `.where('category', category)` to a shared base query, and uses `.clone()` so both the data fetch and count query get the filter.
- **`src/routes/orders.js:13`** — Reads `req.query.category` and passes it through.

When `category` is omitted, behavior is unchanged.
