Done. Here's what I changed:

1. **`src/services/order-service.js`** — `listOrders` now joins the `users` table on `orders.user_id = users.id` and selects `users.name as user_name` alongside all order fields.

2. **`src/routes/orders.js`** — The `GET /api/orders` response now includes a `customer` field with the user's name for each order.
