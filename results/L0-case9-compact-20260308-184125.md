Found **4 memory leaks** — none are database connection issues. They're all unbounded in-memory data structures:

1. **`requestLog` array** (`src/middleware/rate-limiter.js`) — every request is pushed, never pruned. This is likely your main culprit.
2. **`recentErrors` array** (`src/middleware/logger.js`) — every error response is pushed, never pruned.
3. **`priceCache` object** (`src/services/product-service.js`) — cached forever, no TTL or size limit.
4. **`userCache` Map** (`src/services/user-service.js`) — cached forever, no eviction.

The DB connection is fine — `better-sqlite3` uses a single connection, no pooling issues.

Want me to fix all four? The approach would be:
- Cap `requestLog` and `recentErrors` to their last N entries (they already only return the last 100/50 via `.slice()` but never actually trim)
- Add max-size eviction to `priceCache` and `userCache`
