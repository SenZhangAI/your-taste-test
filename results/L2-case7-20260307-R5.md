Changes made to `src/routes/orders.js`:

- **`quantity`** — now required, must be a positive integer (rejects 0, negatives, floats, strings)
- **`total_cents`** — split out of the truthy check into an explicit positive integer check (the old `!total_cents` would reject `0` but accept `-5` or `"abc"`)
