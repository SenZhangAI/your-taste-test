Fixed. The `validateId` function was already imported but never used. Both `GET /:id` and `DELETE /:id` now validate the ID parameter before querying the database:

- **Non-numeric IDs** (like `abc`) → `400 { error: "Invalid order ID" }`
- **Zero or negative IDs** → `400 { error: "Invalid order ID" }`
- **Valid numeric IDs that don't exist** → `404 { error: "Order not found" }` (unchanged)
