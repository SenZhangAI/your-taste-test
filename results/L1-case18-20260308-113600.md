Done. Changes:

1. **`order-service.js`**: `listOrders` now joins the `users` table to fetch `users.name as user_name` in a single query (no N+1).
2. **`routes/orders.js`**: The list response includes `user` field with the user's name.

The join also properly qualifies column references with `orders.` to avoid ambiguity (both tables have `status`, `id`, `created_at`).
