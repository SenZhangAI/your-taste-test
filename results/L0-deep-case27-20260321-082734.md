Done. Here's what changed:

**`src/routes/orders.js`** — 4 endpoints updated:
- **GET /api/orders** — `id: o.id` → `order_id: o.id`
- **GET /api/orders/:id** — destructure `id` from the spread and emit as `order_id`
- **POST /api/orders** — same destructure/rename on the created order
- **GET /api/orders/search** — `id: o.id` → `order_id: o.id`

**`src/routes/users.js`** — 1 endpoint updated:
- **GET /api/users/:id** — orders embedded in user response now have `order_id` instead of `id`

No changes to database schema, service layer, route params, or foreign keys — the rename is purely in the API response layer.
