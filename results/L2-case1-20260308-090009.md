Done. `GET /api/orders?category=electronics` now filters by the `category` column.

Also fixed a bug in the count query — it previously ignored `status` and `category` filters, so pagination `total` would be wrong when filtering. Now the count query reuses the same filtered base query.
