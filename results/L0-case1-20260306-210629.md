Done. Two changes made:

- **`src/routes/orders.js:12`** — Reads `req.query.category` and passes it to `listOrders`.
- **`src/services/order-service.js:8`** — Accepts `category` param and adds a `.where('category', category)` clause when provided. Uses `.clone()` so the same base query applies to both the data fetch and the count.

Usage: `GET /api/orders?category=electronics` (omit the param to get all orders as before).
