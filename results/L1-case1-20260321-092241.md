Done. Changes:

1. **`src/routes/orders.js:14`** — reads `req.query.category` and passes it to `listOrders`
2. **`src/services/order-service.js:10`** — accepts `category` param, adds `.where('category', category)` when present
3. **Bug fix**: the count query now reuses the filtered `query` instead of a separate unfiltered query, so `total` is correct when filtering by `status` or `category`

Usage: `GET /api/orders?category=electronics` — returns only orders where `category = 'electronics'`.
