Done. `GET /api/orders?category=electronics` now filters by the `category` column. Both the results and the total count are filtered, so pagination stays correct. Without the parameter, all non-deleted orders are returned as before.
