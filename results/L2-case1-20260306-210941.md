Done. `GET /api/orders?category=electronics` now filters by the `category` column. The shared base query uses `.clone()` so the count and data queries stay consistent. When `category` is omitted, behavior is unchanged.
