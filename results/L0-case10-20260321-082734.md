No deps installed, but that's fine — the code is syntactically correct. Here's what was added:

**`PATCH /api/orders/:id/status`** — Updates an order's status.

**Request body:**
```json
{ "status": "confirmed" }
```

**Validation:**
- `status` must be one of: `pending`, `confirmed`, `shipped`, `delivered`, `cancelled`
- Returns 404 if order doesn't exist or is soft-deleted
- Returns 400 if the order is already `cancelled` or `delivered` (terminal states)

**Changes made:**
- `src/services/order-service.js` — added `updateOrderStatus(id, status)` function
- `src/routes/orders.js` — added PATCH `/:id/status` route with validation and imported the new service function
