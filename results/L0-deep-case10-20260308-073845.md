Done. Here's what was added:

**`PATCH /orders/:id/status`** — accepts `{ "status": "<new_status>" }` in the request body.

- Validates the status against `ORDER_STATUSES` (`pending`, `confirmed`, `shipped`, `delivered`, `cancelled`). Returns 400 if invalid.
- Returns 404 if the order doesn't exist or is soft-deleted.
- Updates the status and `updated_at` timestamp, then returns the updated order.

**Service layer** (`src/services/order-service.js:55-61`) — new `updateOrderStatus(id, status)` function that performs the DB update.
