Updated `RATE_LIMIT` from 100 to 500 in `src/config.js:2`. The sliding-window rate limiter in `src/middleware/rate-limiter.js` already uses a 1-minute window, so this now allows 500 requests per minute per IP.
