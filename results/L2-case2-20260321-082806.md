Done. Here's a summary of all changes:

**`src/services/order-service.js`:**
- `listOrders` — `where('status', '!=', 'deleted')` → `whereNull('deleted_at')` (both query and count)
- `searchOrders` — same switch
- `getOrder` — added `whereNull('deleted_at')` filter (was missing, now consistent)
- `softDeleteOrder` — sets `deleted_at` timestamp instead of `status: 'deleted'`

**`src/routes/orders.js`:**
- GET `/:id` and DELETE `/:id` — removed redundant `order.status === 'deleted'` checks (now handled by `getOrder` filtering)
- GET `/stats` — `where('status', '!=', 'deleted')` → `whereNull('deleted_at')`

This now aligns with the pattern `user-service.js` already uses for `getUserOrders()`. The `deleted` value is no longer used as a status — orders keep their real status (`pending`, `confirmed`, etc.) after soft-delete, which is cleaner for analytics.
