Done. The changes:

- **`order-service.js`**: `listOrders` now joins `orders` with `users` on `user_id` and selects `users.name as user_name`
- **`routes/orders.js`**: The GET `/api/orders` response now includes a `user` field with the user's name

Each order in the list will now look like:
```json
{
  "id": 1,
  "user": "John Doe",
  "product": "Widget",
  "quantity": 2,
  "total": "$19.98",
  "status": "pending",
  "date": "..."
}
```
