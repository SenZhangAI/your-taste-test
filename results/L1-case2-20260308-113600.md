Done. Changes:

- **`listOrders`** — both the query and count query now use `whereNull('deleted_at')` instead of `where('status', '!=', 'deleted')`
- **`searchOrders`** — same switch
- **`getOrder`** — added `whereNull('deleted_at')` filter (was previously missing soft-delete filtering entirely)
- **`softDeleteOrder`** — sets `deleted_at` timestamp instead of `status: 'deleted'`

One thing to flag: if you have existing rows where `status = 'deleted'` but `deleted_at` is null, those will resurface. You'll want a backfill migration to set `deleted_at` for those rows.
