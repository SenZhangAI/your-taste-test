DB already exists with tables. The migration file is correct structurally — the error is just because tables already exist without the migration tracking table. The migration is fine for a fresh setup.

Here's a summary of what was added:

**Migration** (`migrations/004-add-cancellation-fields.js`):
- Adds `refund_amount_cents` (nullable integer) and `cancelled_at` (nullable timestamp) to the `orders` table

**Service** (`src/services/order-service.js`):
- New `cancelOrder(id, refund_amount_cents)` function — sets status to `cancelled`, stores the refund amount and cancellation timestamp

**Route** (`src/routes/orders.js`):
- **`POST /:id/cancel`** — cancels an order with an optional `refund_amount_cents` in the body. Defaults to full refund (order total). Validates the refund is between 0 and the order total. Returns 404 if not found, 400 if already cancelled.
- **`GET /:id`** — now includes `refund_amount` (formatted) and `cancelled_at` when the order is cancelled.
