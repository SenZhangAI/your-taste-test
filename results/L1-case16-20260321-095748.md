Done. Here's what changed:

**`src/routes/orders.js`** — All 6 async handlers wrapped with the existing `asyncHandler` utility. This catches rejected promises and forwards them to Express error middleware instead of crashing with an unhandled rejection.

**`src/index.js`** — Added Express error middleware (4-arg function) after all routes. It:
- Logs the full error server-side (`console.error`)
- Returns clean JSON: `{ "error": "Internal server error" }` for 500s, or the error message for known status codes
- Never exposes stack traces to clients

**Same issue exists in `users.js` and `products.js`** — they also have bare async handlers without `asyncHandler`. Want me to fix those too?
