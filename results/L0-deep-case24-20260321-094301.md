Everything looks good. Here's what was added:

**Migration** (`migrations/004-add-cancellation-fields.js`):
- `refund_amount_cents` (integer, nullable) — tracks the refund amount
- `cancelled_at` (timestamp, nullable) — when the cancellation happened

**Service** (`src/services/order-service.js`):
- `cancelOrder(id, refund_amount_cents)` — sets status to `cancelled`, stores the refund amount and timestamp

**Route** (`src/routes/orders.js`):
- `POST /orders/:id/cancel` — accepts `{ refund_amount_cents }` in the body. Validates that:
  - Order exists and isn't deleted
  - Order isn't already cancelled
  - `refund_amount_cents` is provided and non-negative
  - Refund doesn't exceed the order total (supports partial refunds)
- `GET /orders/:id` — now includes `refund_amount_cents`, `refund_amount` (formatted), and `cancelled_at` when the order is cancelled
