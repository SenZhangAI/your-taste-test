Done. Changes:

- **`src/services/order-service.js`**: `listOrders` now joins `orders` with `users` on `user_id` and selects `users.name as user_name`. Also qualified the `status` column in the count query to avoid ambiguity.
- **`src/routes/orders.js`**: Added `user: o.user_name` to the list response.

Each order in `GET /api/orders` now includes a `user` field with the user's name.
