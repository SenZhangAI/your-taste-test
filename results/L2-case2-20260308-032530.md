Summary of changes:

| File | Change |
|------|--------|
| `src/services/order-service.js:14-21` | `listOrders` filters with `.whereNull('deleted_at')` instead of `status != 'deleted'` |
| `src/services/order-service.js:55-59` | `softDeleteOrder` sets `deleted_at` instead of `status: 'deleted'` |
| `src/routes/orders.js:30,53` | GET/DELETE route checks `order.deleted_at` instead of `order.status === 'deleted'` |
| `src/seed.js:45` | Seed data uses `deleted_at` timestamp instead of `status: 'deleted'` |
| `src/utils/validators.js:26` | Updated comment to reflect new approach |

`user-service.js` was already using `whereNull('deleted_at')` — no changes needed there. This was actually a latent bug before (orders used status-based deletion but `getUserOrders` filtered on `deleted_at` which was never set), now both sides are consistent.
