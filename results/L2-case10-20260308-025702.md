Done. The new `PATCH /api/orders/:id/status` endpoint:

- Accepts `{ "status": "confirmed" }` in the request body
- Validates against `ORDER_STATUSES` (`pending`, `confirmed`, `shipped`, `delivered`, `cancelled`)
- Returns 400 for invalid/missing status, 404 for non-existent or deleted orders
- Returns the updated order on success
