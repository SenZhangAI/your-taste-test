All done. Here's a summary of the changes across both files:

**`src/services/order-service.js`:**
- `listOrders` — both the query and count now use `.whereNull('deleted_at')` instead of `.where('status', '!=', 'deleted')`
- `searchOrders` — same swap
- `softDeleteOrder` — now sets `deleted_at: db.fn.now()` instead of `status: 'deleted'`

**`src/routes/orders.js`:**
- `GET /:id` and `DELETE /:id` — guard checks changed from `order.status === 'deleted'` to `order.deleted_at`
- `GET /stats` — query uses `.whereNull('deleted_at')`

This aligns orders with the same timestamp-based soft-delete pattern already used by the user service.
