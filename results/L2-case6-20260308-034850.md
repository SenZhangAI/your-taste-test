Two fixes applied:

1. **`order-service.js:32`** — `getOrder` now filters `whereNot('status', 'deleted')`, matching its JSDoc contract. This is the authoritative fix — the route-level checks in `orders.js:30` and `orders.js:53` are now redundant but harmless.

2. **`user-service.js:36`** — `getUserOrders` was using `whereNull('deleted_at')` to filter deleted orders, but orders use **status-based** soft-delete (never set `deleted_at`). Changed to `whereNot('status', 'deleted')` to match the actual deletion mechanism. This was silently returning deleted orders to user detail/notification endpoints.

Note: `listUsers` at `user-service.js:25` correctly uses `whereNull('deleted_at')` — the `users` table does use `deleted_at`-based soft-delete (per `db.js:19`), so that's fine.
