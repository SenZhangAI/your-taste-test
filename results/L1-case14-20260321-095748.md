Done. Two issues fixed:

1. **POST `/api/orders`** — added `total` and `date` formatting to match GET responses (line 49-53).

2. **Cents/dollars bug in all handlers** — `getOrderTotal()` returns cents (per its docstring: `total_cents * quantity`), but `formatPrice()` expects dollars. Every existing caller was passing cents directly, so `total_cents: 2999` would render as `"$2999.00"` instead of `"$29.99"`. Added `/ 100` at all 4 call sites (lines 21, 37, 51, 64).

Note: the `/stats` endpoint's `revenue` field (line 74) still returns raw cents — that's an admin/internal value so I left it as-is, but worth deciding if that should also be formatted.
