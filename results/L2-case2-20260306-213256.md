Updated all 5 files:

- **`src/services/order-service.js`** — `listOrders` queries use `.whereNull('deleted_at')`; `softDeleteOrder` sets `deleted_at` instead of `status: 'deleted'`
- **`src/routes/orders.js`** — GET `/:id` and DELETE `/:id` check `order.deleted_at` instead of `order.status === 'deleted'`
- **`src/routes/users.js`** — list users query uses `.whereNull('deleted_at')`; GET `/:id` checks `user.deleted_at`; user's orders query uses `.whereNull('deleted_at')`
- **`src/routes/products.js`** — list products query uses `.whereNull('deleted_at')`; GET `/:id` checks `product.deleted_at`
- **`src/seed.js`** — deleted orders now use `deleted_at` timestamp instead of `status: 'deleted'`

Note: the prompt only mentioned orders, but users and products had the same `status !== 'deleted'` pattern — migrated all three to keep the soft-delete approach consistent across the codebase.
