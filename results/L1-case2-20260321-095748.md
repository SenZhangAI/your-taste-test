All done. Here's the summary of changes:

**`src/services/order-service.js`:**
- `listOrders` — both the query and count query now use `.whereNull('deleted_at')`
- `searchOrders` — same filter swap
- `getOrder` — added `.whereNull('deleted_at')` so the soft-delete filter lives in the service, not scattered across routes
- `softDeleteOrder` — sets `deleted_at` timestamp instead of `status: 'deleted'`

**`src/routes/orders.js`:**
- GET `/:id` and DELETE `/:id` — removed redundant `order.status === 'deleted'` checks (now handled by `getOrder`)
- GET `/stats` — inline query switched to `.whereNull('deleted_at')`

One thing to flag: the `/stats` endpoint has an inline query that bypasses the service layer. If you're planning more soft-delete changes, worth pulling that into the service too.
