Changes made:

- **`src/utils/validators.js:16`** — Added `quantity` param and validation: `quantity must be a positive integer` (rejects 0, negatives, floats, and non-numbers)
- **`src/routes/orders.js:45`** — Replaced the inline check with the existing `validateOrderPayload` function, which was imported but never used

Now `POST /orders` with `quantity: 0` returns `400 { error: "quantity must be a positive integer" }`.
