**Breadth-scan:** The `/stats` endpoint on line 70 also computes `o.total_cents * o.quantity` directly — that `revenue` field stays in cents, which is fine for a stats/admin endpoint (no `formatPrice` call). No other callers of `formatPrice` or `getOrderTotal` exist elsewhere.

**Summary:** `getOrderTotal` correctly returns cents, `formatPrice` correctly formats dollars, but the three call sites in `routes/orders.js` were passing cents straight to `formatPrice` without dividing by 100. Prices were displaying 100x too high (e.g. `$2598.00` instead of `$25.98`).
