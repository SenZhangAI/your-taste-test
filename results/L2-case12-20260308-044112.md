Fixed. Both `GET /:id` and `DELETE /:id` now validate the ID parameter using the existing `validateId` utility and return a `400` with `"Invalid order ID"` for non-numeric/invalid IDs like `"abc"`. The validated numeric ID is passed downstream instead of the raw string.
