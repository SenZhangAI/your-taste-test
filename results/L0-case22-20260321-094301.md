The query itself is fine — the problem was that Express was treating `"search"` as an `:id` parameter. The `/:id` wildcard route was grabbing the request before it could reach `/search`. Moved `/search` above `/:id` so it matches first. The same issue would affect `/stats` (line 66), which is also below `/:id` — you may want to be aware of that too.
