Done. The list orders endpoint now joins with the `users` table via `LEFT JOIN` and includes a `user` field (the user's name) in each order. Used `LEFT JOIN` so orders with a missing/deleted user still show up (as "Unknown") rather than disappearing from the list.
