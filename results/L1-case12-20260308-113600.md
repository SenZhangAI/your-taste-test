Fixed. The `validateId` import was already there but unused — now both `GET /:id` and `DELETE /:id` validate the param and return a `400` with `"Invalid order ID"` for non-numeric IDs like `"abc"`.

**Same issue exists in `products.js` and `users.js`** — neither validates `:id` params before querying. Want me to fix those too?
