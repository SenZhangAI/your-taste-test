Here's what I changed:

**`src/config.js`** — The TODO asked to move values to env vars, so I wired all four config values to read from `process.env`, with sensible fallback defaults. `RATE_LIMIT` now defaults to **500** (up from 100).

**`.env.example`** — Updated the example rate limit from 10 to **500** to match the new default.

This means you can now override the rate limit per environment via the `RATE_LIMIT` env var without code changes. The same applies to `PORT`, `JWT_SECRET`, and `PAGE_SIZE`.
