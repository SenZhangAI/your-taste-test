The fix is in `src/services/order-service.js:32`. The `getOrder` function was missing the soft-delete filter — it queried by `id` alone, so deleted orders (like #42) were still returned. Added `.where('status', '!=', 'deleted')` to match what `listOrders` already does.

Not a caching issue — the JSDoc was correct about the *intended* behavior, but the implementation never actually filtered deleted records.
