Both `GET /api/orders/:id` and `DELETE /api/orders/:id` now validate the ID parameter using the already-imported `validateId` helper. Non-numeric or non-positive IDs like `"abc"` will return a `400` with `{"error": "Invalid order ID"}` instead of hitting the database and causing a 500.
