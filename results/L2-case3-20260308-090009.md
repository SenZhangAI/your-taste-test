**Root cause**: `config.js` was hardcoding `RATE_LIMIT = 100` and ignoring `process.env`. The `.env` file had no effect at all.

**What changed**:
- `config.js`: now reads `process.env.RATE_LIMIT`, defaults to 500 if unset
- `.env.example`: updated to 500 to match the new default

Also worth noting: `rate-limiter.js:29` has an unbounded `requestLog` array that grows forever — it'll leak memory in production. Want me to fix that too?
