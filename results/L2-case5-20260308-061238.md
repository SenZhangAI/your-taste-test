Done. `GET /api/orders/export` queries all non-deleted orders directly from the DB (no pagination loop) and returns a CSV file. The route is placed before `/:id` so Express doesn't treat "export" as an order ID.
