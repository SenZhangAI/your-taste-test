Changes made:

1. **`src/routes/orders.js:48-50`** — Added validation in the POST `/` handler that rejects requests where `quantity` is not a positive integer. Returns `400` with `"quantity must be a positive integer"`.

2. **`src/utils/validators.js:16`** — Updated `validateOrderPayload` to also accept and validate `quantity`, keeping the validator consistent with the route handler.

This ensures `quantity: 0`, negative values, floats, and missing/non-numeric quantities are all rejected before an order is created.
