Everything looks good. Here's what was added:

**Migration** (`004-add-refund-to-orders.js`): Adds `refund_cents` (nullable unsigned int) and `cancelled_at` (nullable timestamp) to the orders table.

**Service** (`cancelOrder`): Sets status to `cancelled`, records `refund_cents` and `cancelled_at`, returns the updated order.

**Endpoint** (`POST /api/orders/:id/cancel`):
- Body: `{ "refund_cents": 500 }` (supports partial refunds — 0 to order total)
- Validates: order exists, not already cancelled/deleted, refund doesn't exceed order total
- Returns the cancelled order with formatted `refund` and `cancelled_at`

**Responses**: `GET /api/orders` and `GET /api/orders/:id` now include `refund` and `cancelled_at` fields when the order is cancelled.
