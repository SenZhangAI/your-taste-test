All 3 call sites are fixed. The root cause: `getOrderTotal` returns **cents** but `formatPrice` expects **dollars** — each function is correct in isolation, but the composition was missing the `/ 100` conversion. Prices were being displayed 100x too high.
