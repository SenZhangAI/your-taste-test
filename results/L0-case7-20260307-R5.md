The fix adds a validation check at `src/routes/orders.js:44` that rejects orders where `quantity` is not a positive integer. This covers `0`, negative numbers, non-integers like `1.5`, and non-numeric values.
