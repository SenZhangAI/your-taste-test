Now `query` already carries both the `status != 'deleted'` base condition and the optional `status` filter, so cloning it for the count ensures the total always matches the filtered dataset.

**Breadth-scan**: Also spotted a secondary bug — `getOrderTotal()` returns cents (line 55-56, JSDoc confirms "Total in cents"), but `formatPrice()` in `src/utils/format.js` treats the value as dollars (`$${dollars.toFixed(2)}`). An order of 250 cents would display as `$250.00` instead of `$2.50`. Want me to fix that too?
