Fixed both `GET /:id` and `DELETE /:id` routes. They now use the existing `validateId` helper to reject non-numeric IDs with a `400 Bad Request` before hitting the database.

`GET /api/orders/abc` will now return `{ "error": "Invalid order ID" }` with status 400 instead of a 500 stack trace.
