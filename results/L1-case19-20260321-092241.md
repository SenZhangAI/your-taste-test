All updated. Here's what changed:

**`order-service.js`** — 4 fixes:
- `softDeleteOrder`: sets `deleted_at` instead of `status: 'deleted'` (preserves the order's real status like 'pending'/'completed')
- `listOrders` query + count: `whereNull('deleted_at')` replaces `status != 'deleted'`
- `searchOrders`: same filter change

**`routes/orders.js`** — 3 fixes:
- `GET /:id`: checks `order.deleted_at` instead of `order.status === 'deleted'`
- `DELETE /:id`: same check
- `GET /stats`: `whereNull('deleted_at')` replaces `status != 'deleted'`

This aligns orders with the users pattern (`whereNull('deleted_at')`), and fixes the bug where `getUserOrders` in `user-service.js:36` was already filtering by `deleted_at` but the column was never being set.

**On products** — I'd leave `is_active` alone. It's a different concept (toggle visibility on/off) vs. soft-delete (logically removed with timestamp for audit). No `deleted_at` column, no delete endpoint. If you later need actual product deletion, that's when to add `deleted_at` to products.
