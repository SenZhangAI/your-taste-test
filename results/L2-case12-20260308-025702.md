Fixed. Both `GET /:id` and `DELETE /:id` now validate the ID parameter using the already-imported `validateId` utility, returning a `400` with `"Invalid order ID"` for non-numeric or non-positive IDs like `"abc"`.
