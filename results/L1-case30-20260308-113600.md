Module loads fine. Here's what was added:

**`POST /api/orders/bulk`** — accepts `{ "orders": [...] }` in the request body.

**Route** (`src/routes/orders.js:52-68`):
- Validates the body contains a non-empty `orders` array
- Validates each order has `user_id`, `product_name`, `total_cents`
- Returns per-item error details with index on validation failure
- Returns `201` with all created orders on success

**Service** (`src/services/order-service.js:59-73`):
- `createBulkOrders()` wraps all inserts in a single transaction — if any insert fails, the entire batch rolls back
- Returns the full order objects after insert

Example request:
```json
POST /api/orders/bulk
{
  "orders": [
    { "user_id": 1, "product_name": "Widget", "quantity": 2, "total_cents": 1999 },
    { "user_id": 2, "product_name": "Gadget", "total_cents": 4999 }
  ]
}
```
