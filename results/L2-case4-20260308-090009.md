All 3 call sites fixed. Also noting the `/stats` endpoint at line 70 has the same raw cents calculation (`o.total_cents * o.quantity`) — that one returns `revenue` as a raw number rather than a formatted string, so it's consistently in cents. If the frontend displays that as dollars, the same conversion would be needed there too, but that depends on what the consumer expects.

**Root cause:** `getOrderTotal` returns cents, `formatPrice` expects dollars, and all call sites were passing the value straight through without the `/ 100` conversion.
