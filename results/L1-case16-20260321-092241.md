Done. Here's what changed:

**`src/routes/orders.js`** — All 6 async route handlers wrapped with the existing `asyncHandler` from `src/utils/async-handler.js`. This catches any rejected promise and forwards it to Express error middleware via `next(err)`.

**`src/index.js`** — Added a global error handler middleware (registered after all routes) that:
- Logs the full error server-side (`console.error`)
- Returns a clean JSON response: `{ "error": "Internal server error" }` for 5xx, or the error message for 4xx
- Never exposes stack traces to clients

**Breadth-scan note:** The `users.js` and `products.js` routes have the same unguarded async handlers. They'll benefit from the same `asyncHandler` treatment when you're ready.
