**The bug:** The count query (line 25) was building a fresh `db('orders')` query that only excluded deleted orders, ignoring the `status` filter entirely. So it always returned the total of *all* non-deleted orders (e.g., 47) regardless of the active filter.

**The fix:** Reuse `query.clone()` (which already has the status filter applied) for the count query, so the total reflects the filtered result set.
