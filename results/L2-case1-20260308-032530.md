Done. The `?category=electronics` filter is now supported on `GET /api/orders`. It applies an exact match on the indexed `category` column, filtering both the results and the total count. When omitted, all non-deleted orders are returned as before.
