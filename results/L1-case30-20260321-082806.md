Done. Here's what was added:

**Route** (`src/routes/orders.js:53-71`) — `POST /api/orders/bulk`
- Accepts `{ orders: [...] }` in the request body
- Validates the array is non-empty and each order has `user_id`, `product_name`, `total_cents`
- Returns 400 with per-index error details on validation failure
- Returns 201 with `{ orders: [...] }` on success

**Service** (`src/services/order-service.js:71-88`) — `createBulkOrders()`
- Wraps all inserts in a single Knex transaction — if any insert fails, all are rolled back
- Returns the full created order objects (matching `createOrder` behavior)

Example request:
```json
POST /api/orders/bulk
{
  "orders": [
    { "user_id": 1, "product_name": "Widget", "total_cents": 999, "quantity": 2 },
    { "user_id": 1, "product_name": "Gadget", "total_cents": 1499 }
  ]
}
```
