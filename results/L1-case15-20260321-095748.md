Fixed. The issue was in `src/config.js:2` — `RATE_LIMIT` was hardcoded to `100` instead of reading from `process.env.RATE_LIMIT`. Now it parses the env var as an integer, falling back to `100` if unset.

**Breadth-scan:** `PORT` already reads from `process.env` correctly. `PAGE_SIZE` is also hardcoded (`50`) but `.env.example` doesn't list it, so that appears intentional. No other config values are affected.

One thing to verify: make sure your app loads `.env` (e.g., via `dotenv`) before `config.js` is imported. Check your `src/index.js` entry point for a `dotenv.config()` call — if it's missing, the env var still won't be read.
