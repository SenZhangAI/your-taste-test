The seed confirms the issue: `total_cents: product.price` stores the **per-unit** price in cents, but `getOrderTotal` multiplies it by `quantity` again. So for qty 1 it happens to equal the unit price, but for higher quantities it's correct — `total_cents` is actually the unit price, not the line total.

Wait — the field is named `total_cents` but it stores the **unit price**. The multiplication in `getOrderTotal` is intentionally computing the line total. So `getOrderTotal` is correct — the only bug is the missing `/100` conversion. The fix I already applied is the right one.

**Summary:** The order routes were passing cents directly to `formatPrice()` which expects dollars. Widget Pro at 2999 cents was displayed as `$2999.00` instead of `$29.99`. Added `/ 100` in both the list and detail endpoints, matching the pattern already used in the product routes.
