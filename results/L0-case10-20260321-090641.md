Done. Here's what was added:

- **`PATCH /orders/:id/status`** — accepts `{ "status": "confirmed" }` in the request body. Validates that the status is one of the allowed values (`pending`, `confirmed`, `shipped`, `delivered`, `cancelled`), returns 404 if the order doesn't exist or is deleted, and returns the updated order on success.
- **`updateOrderStatus(id, status)`** in `order-service.js` — updates the status and `updated_at` timestamp, then returns the updated order.
