The fix: `SORTABLE_FIELDS` is now a map from user-facing sort names to actual DB columns. `price` maps to the `total_cents` column (which is numeric), so the database performs a proper numeric sort. Previously, `price` wasn't in the allowed list, so it silently fell back to `created_at` ordering — that's why cheap items appeared after expensive ones (they just happened to be created later).
