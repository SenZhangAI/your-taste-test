Here's what the stock check does:

1. **Looks up the product by name** — returns 400 if not found
2. **Checks `product.stock >= quantity`** — returns 409 (Conflict) with available/requested counts if insufficient
3. **Decrements stock** after inserting the order
4. **Auto-fills `product_id`** from the looked-up product if not explicitly provided
