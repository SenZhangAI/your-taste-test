Fixed. Both `GET /:id` and `DELETE /:id` now validate the ID parameter using the already-imported `validateId`, returning a `400` with `"Invalid order ID"` for non-numeric/invalid IDs like `"abc"`. The validated numeric ID is then passed downstream instead of the raw string.
