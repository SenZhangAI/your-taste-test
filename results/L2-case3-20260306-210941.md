Changed the rate limit from 100 to 500 requests per minute in `src/config.js:4`. The rate limiting middleware in `src/index.js:19` already uses this constant with a 60-second sliding window, so no other changes needed.
