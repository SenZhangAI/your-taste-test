Here's what I added:

**Migration** (`migrations/004-add-cancellation-fields.js`): Adds `refund_amount_cents` and `cancelled_at` columns to the orders table.

**Service** (`src/services/order-service.js`): New `cancelOrder(id, { refund_amount_cents })` function that sets status to `cancelled`, stores the refund amount, and timestamps the cancellation.

**Route** (`src/routes/orders.js`):
- `POST /:id/cancel` — Cancels an order. Accepts optional `refund_amount_cents` in the body (defaults to full order total for a full refund). Validates the refund is between 0 and the order total. Returns 400 if already cancelled.
- `GET /:id` — Now includes `refund` and `cancelled_at` fields when the order is cancelled.
