Found **4 memory leaks** — it's not database connections, it's unbounded in-memory data structures.

### Critical leaks

1. **`requestLog` array** (`src/middleware/rate-limiter.js:4`) — every request pushes to this array, never pruned. This is almost certainly your main culprit.

2. **`recentErrors` array** (`src/middleware/logger.js:6`) — every 4xx/5xx response appends here, never cleaned up.

### Minor leaks

3. **`priceCache` object** (`src/services/product-service.js:7`) — cached forever, never invalidated (also serves stale prices after updates).

4. **`userCache` Map** (`src/services/user-service.js:3`) — same pattern, no TTL or eviction.

All four use the same anti-pattern: append/set without any cap or cleanup. The `getRequestStats()` and `getRecentErrors()` functions only *read* the last N entries but never *trim* the underlying storage.

Want me to fix all four? The approach would be:
- **requestLog / recentErrors**: cap the arrays (e.g. keep last 1000/50, splice old entries)
- **priceCache / userCache**: add simple TTL expiry and invalidate on mutations
