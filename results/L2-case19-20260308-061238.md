Done. Added `deleted_at: db.fn.now()` to `softDeleteOrder` at `src/services/order-service.js:58`.

The existing `status: 'deleted'` stays — all query filters (`listOrders`, `user-service.js`) check `status`, so they keep working. `deleted_at` now records *when* the deletion happened.

Re: products — that table uses `is_active` boolean with no `deleted_at` column. It's a different (simpler) pattern, not a broken one. Aligning them would mean adding a column + migration + rewriting product queries, all for no functional benefit. I'd leave it as-is unless you're planning to standardize soft-delete across all tables as a deliberate migration.
