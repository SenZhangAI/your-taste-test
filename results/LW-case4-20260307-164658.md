Fixed both places in `src/routes/orders.js` where `getOrderTotal()` (which returns cents) was passed directly to `formatPrice()` (which expects dollars). Added `/ 100` to convert cents to dollars, matching the pattern already used in `src/routes/products.js:14`.

Widget Pro will now display as `$29.99` instead of `$2999.00`.
