All four route handlers now have try/catch blocks. Database errors will return `{ "error": "Failed to ..." }` with a 500 status instead of leaking stack traces. The full error is still logged server-side via `console.error` for debugging.
