The fix adds a validation check at `src/routes/orders.js:46` that rejects orders where `quantity` is not a positive integer (covers `0`, negative numbers, decimals, `null`, `undefined`, strings, etc.). Orders must now have `quantity >= 1`.
