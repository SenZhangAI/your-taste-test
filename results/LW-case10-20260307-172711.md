Done. Here's what was added:

- **`PATCH /api/orders/:id/status`** — accepts `{ "status": "<value>" }` in the request body
  - Validates the status against `ORDER_STATUSES` (`pending`, `confirmed`, `shipped`, `delivered`, `cancelled`)
  - Returns 400 if the status is missing or invalid
  - Returns 404 if the order doesn't exist or is soft-deleted
  - Returns the updated order on success
- **`updateOrderStatus(id, status)`** in `src/services/order-service.js:55` — updates the status and `updated_at` timestamp, then returns the updated order
