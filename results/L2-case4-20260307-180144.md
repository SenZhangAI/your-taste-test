Fixed both occurrences in `src/routes/orders.js`. The issue: `getOrderTotal()` returns cents (2999), but `formatPrice()` expects dollars. The route was passing cents directly, so $29.99 displayed as $2999.00.

This matches how the products route already handles it — `formatPrice(p.price_cents / 100)`.
