**Summary:**

1. **The reported bug couldn't reproduce with current code** — the route handler already had a compensating check at `orders.js:30`. The customer report may be stale or inaccurate.

2. **The real problem was a lying contract** — `getOrder`'s JSDoc promised deleted-record filtering, but the implementation didn't deliver. Every caller had to independently remember to check `status === 'deleted'`, which is fragile.

3. **Fix:** Added `.whereNot({ status: 'deleted' })` to `getOrder` (service layer), making the JSDoc truthful. Removed the now-redundant `order.status === 'deleted'` checks from both route handlers. The service owns the soft-delete contract — callers just check for `null`.
