Fixed:

1. **`src/utils/validators.js:16`** — `validateOrderPayload` now checks `quantity` is a positive integer (`>= 1`)
2. **`src/routes/orders.js:43-46`** — The create route now uses `validateOrderPayload` instead of the ad-hoc inline check, so all validation (including quantity) is in one place

Requests with `quantity: 0`, negative, missing, or non-integer values will get a `400` with a descriptive error message.
