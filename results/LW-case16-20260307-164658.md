All four order route handlers (`GET /`, `GET /:id`, `POST /`, `DELETE /:id`) now have try/catch blocks that:

- Log the full error to the server console for debugging
- Return a clean `{ "error": "..." }` JSON response with a 500 status
- Never expose stack traces or internal details to the client
