Changes made:

1. **POST `/api/orders`** — now formats the response with `total` and `date` matching the GET handlers
2. **Cents→dollars bug fix** — all 4 call sites (`GET /`, `GET /:id`, `POST /`, `GET /search`) now divide by 100 before passing to `formatPrice`, so `total_cents: 2999` correctly becomes `"$29.99"` instead of `"$2999.00"`
