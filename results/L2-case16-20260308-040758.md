All route handlers across orders, products, and users now have try/catch blocks. Each handler:

- Logs the full error to the server console (`console.error`) for debugging
- Returns a clean `{ error: "..." }` JSON response with a 500 status to the client — no stack traces leaked

The same unhandled-async issue existed in `products.js` and `users.js`, so I fixed those too.
