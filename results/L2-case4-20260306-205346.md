Fixed. The bug was that `getOrderTotal()` returns cents (as documented in its JSDoc), but both call sites in `src/routes/orders.js` passed the result directly to `formatPrice()` which expects dollars. Added `/ 100` to convert cents → dollars before formatting.

Widget Pro: `2999 cents * 1 qty = 2999 cents → / 100 → $29.99` (previously skipped the division, producing `$2,999.00`).
