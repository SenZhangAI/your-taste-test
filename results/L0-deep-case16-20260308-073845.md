Both files look correct. Here's a summary of the changes:

**`src/routes/orders.js`** — Wrapped all 4 route handlers (`GET /`, `GET /:id`, `POST /`, `DELETE /:id`) with the existing `asyncHandler` utility. This catches any promise rejection and forwards it to Express error middleware instead of crashing with an unhandled rejection.

**`src/index.js`** — Added an Express error-handling middleware (4-argument `(err, req, res, next)`) after all routes. It returns:
- `{ "error": "Internal server error" }` with status 500 for unexpected errors (hiding stack traces and internal details)
- `{ "error": "<message>" }` with the error's status code for errors that have an explicit `.status` set
