Changes made to `src/routes/orders.js`:

1. **`quantity`** — now required and must be a positive integer (rejects 0, negatives, floats, strings)
2. **`total_cents`** — now validated as a positive integer instead of just truthy (the old `!total_cents` check would reject `0` but accept `-5` or `"abc"`)
3. Split the original combined check so `user_id`/`product_name` presence is checked separately from the numeric validations, giving callers specific error messages
