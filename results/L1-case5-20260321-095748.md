Done. Here's what I did:

**`src/services/order-service.js`** — Added `getAllOrders({ sort, status })` that queries all non-deleted orders in one shot, no pagination. Reuses the same sort/status filter logic as `listOrders`.

**`src/routes/orders.js`** — Added `GET /api/orders/export` that:
- Queries all orders via `getAllOrders`
- Streams CSV with `Content-Disposition: attachment` headers
- Supports `?status=` and `?sort=` query params (same as the list endpoint)
- Uses `res.write()` per row instead of building the full CSV in memory
- Placed **before** the `/:id` route so Express matches it correctly

**Heads up — existing route ordering bug:** The `/search` and `/stats` routes (lines 75, 88) are defined *after* `/:id` (line 52), so `GET /api/orders/search` would match `:id = "search"` and return 404. They should be moved above `/:id`. Want me to fix that?
