Now it reads `RATE_LIMIT` from `process.env` (parsed as an integer), falling back to `100` as the default — consistent with how `PORT` and `JWT_SECRET` are handled. Your `.env` setting of `RATE_LIMIT=10` will now be respected.
