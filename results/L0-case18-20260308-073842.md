Done. The changes:

1. **Imported `getUser`** from `user-service.js` (`orders.js:3`)
2. **Look up user for each order** using `getUser(o.user_id)` and include `user: user.name` in the response (`orders.js:15-25`)

The `getUser` service already has built-in caching, so if multiple orders belong to the same user, the DB is only queried once for that user.
