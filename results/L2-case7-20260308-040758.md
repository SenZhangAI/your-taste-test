Two changes:

1. **`src/utils/validators.js:16`** — Added `quantity` to `validateOrderPayload`: must be a positive integer (`>= 1`). Rejects `0`, negatives, floats, `null`/`undefined`, and non-numeric values.

2. **`src/routes/orders.js:43-47`** — Replaced the inline field check with `validateOrderPayload` (which was already imported but unused), so all validation including quantity runs through one place.
