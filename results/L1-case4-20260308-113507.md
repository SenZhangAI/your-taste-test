Fixed all three call sites (list, get single, search) by dividing by 100 before passing to `formatPrice`.

Note: the `/stats` endpoint on line 70 computes revenue directly as `o.total_cents * o.quantity` — that's still in cents too, but since it's a raw number (not formatted with `formatPrice`), whether that's intentional depends on what the admin dashboard expects. Worth checking.
