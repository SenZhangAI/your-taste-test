The `/api/orders/export` endpoint is added at `src/routes/orders.js:50`. It fetches all non-deleted orders, formats them as CSV with the same field formatting as the list endpoint, and returns with `Content-Type: text/csv` and a download disposition header.

Note: the endpoint is placed **before** the `/:id` route so Express doesn't match "export" as an ID parameter.
