Done. Changes:

- **`src/services/order-service.js`**: `listOrders` now joins `users` on `user_id` and selects `users.name as user_name`
- **`src/routes/orders.js`**: Added `user: o.user_name` to the order list response

Each order in `GET /api/orders` now includes a `user` field with the user's name.
