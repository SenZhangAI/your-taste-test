Done. All 5 order-related API response locations updated:

| Endpoint | File | Change |
|---|---|---|
| `GET /api/orders` | `orders.js:18` | `id` → `order_id` |
| `GET /api/orders/:id` | `orders.js:35-39` | destructure `id` from spread, emit as `order_id` |
| `POST /api/orders` | `orders.js:49` | destructure raw DB `id`, emit as `order_id` |
| `GET /api/orders/search` | `orders.js:58` | `id` → `order_id` |
| `GET /api/users/:id` (nested orders) | `users.js:28` | map embedded orders to rename `id` → `order_id` |

The DB column and internal service layer remain unchanged — only the API response shape is affected. User and product `id` fields are untouched since they're not order-related.
