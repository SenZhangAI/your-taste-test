Done. Two things fixed:

1. **POST `/api/orders`** (line 49-53) — now formats the response with `total` and `date`, matching GET.

2. **Cents→dollars bug** — `getOrderTotal()` returns cents (per its docstring) but `formatPrice()` expects dollars. All 4 call sites were passing cents directly, so an order of $29.99 would display as `$2999.00`. Added `/ 100` at every call site.

Note: the `/stats` endpoint (line 74) keeps `revenue` in raw cents — that's an aggregate for admin, so left it as-is. If you want that formatted too, let me know.
