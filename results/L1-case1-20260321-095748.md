Done. Two changes:

1. **`src/routes/orders.js:14`** — reads `req.query.category` and passes it to `listOrders`
2. **`src/services/order-service.js:10`** — accepts `category` param and adds a `.where('category', category)` clause when present

Usage: `GET /api/orders?category=electronics`

**Bug fix included:** The count query previously ignored all filters (it always counted all non-deleted orders). Now it reuses the same filtered query base, so `total` is accurate when filtering by status, category, or both.
