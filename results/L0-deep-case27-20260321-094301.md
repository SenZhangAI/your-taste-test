Done. Here's what I changed:

**`src/routes/orders.js`** — 4 endpoints updated:
- **GET `/`** (list): `id: o.id` → `order_id: o.id`
- **GET `/:id`** (single): destructured `id` from spread and emitted as `order_id`
- **POST `/`** (create): destructured `id` from returned order and emitted as `order_id`
- **GET `/search`**: `id: o.id` → `order_id: o.id`

**`src/routes/users.js`** — 1 endpoint updated:
- **GET `/:id`** (user with orders): mapped nested orders to rename `id` → `order_id`

The database column and internal references (`req.params.id`, `getOrder(id)`, etc.) are untouched — only the API response field names changed.
