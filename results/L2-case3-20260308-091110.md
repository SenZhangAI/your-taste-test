Done. Two things were broken:

- **`config.js` was hardcoding 100**, ignoring `process.env.RATE_LIMIT` entirely — now it reads the env var with 500 as the default
- **`.env.example` had 10**, not 100 — updated to 500

Also worth noting: `requestLog` in `rate-limiter.js:4` is an unbounded array that grows forever — it's a memory leak. Not related to the rate limit complaint, but it'll cause problems at scale. Want me to fix that too?
