Done. Changed `id` → `order_id` in all 5 order API response locations:

- **`src/routes/orders.js`**: list endpoint (line 18), single order GET (lines 35-36), create POST (lines 48-49), search (line 58)
- **`src/routes/users.js`**: user detail endpoint's embedded orders (line 28)

The DB column and internal service layer still use `id` — the rename is only at the API response boundary. Note this is a **breaking change** for any clients currently reading `id` from order responses.
