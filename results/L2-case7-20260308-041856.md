Two changes:

1. **`src/utils/validators.js:17`** — `validateOrderPayload` now checks `quantity` is a positive integer (`>= 1`)
2. **`src/routes/orders.js:43-46`** — Route handler now uses `validateOrderPayload` instead of the inline check that was missing `quantity`

Requests with `quantity: 0`, negative, or non-integer values will now get a 400 response.
