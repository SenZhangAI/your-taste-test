Done. Added:

- **`PATCH /orders/:id/status`** — accepts `{ "status": "confirmed" }` in the body
- Validates status against `ORDER_STATUSES` (pending, confirmed, shipped, delivered, cancelled)
- Returns 400 for invalid/missing status, 404 for non-existent or deleted orders
- `updateOrderStatus` in order-service updates the status and `updated_at` timestamp, returns the updated order
