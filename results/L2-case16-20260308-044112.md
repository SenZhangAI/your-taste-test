Added try/catch error handling to all async route handlers across all three route files:

- **`src/routes/orders.js`** — all 4 endpoints (GET list, GET by id, POST create, DELETE)
- **`src/routes/users.js`** — both endpoints (GET list, GET by id)
- **`src/routes/products.js`** — both endpoints (GET list, GET by id)

Each handler now catches database errors, logs the full error server-side via `console.error`, and returns a clean `{ error: "..." }` JSON response with a 500 status — no stack traces leak to the client.
