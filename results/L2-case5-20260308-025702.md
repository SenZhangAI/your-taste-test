The `/api/orders/export` endpoint is ready. It:

- Fetches all non-deleted orders via `getAllOrders()`
- Formats them as CSV with columns: `id, product, quantity, total, status, date`
- Returns with `Content-Type: text/csv` and a `Content-Disposition` header for download
