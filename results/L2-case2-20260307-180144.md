Done. Here's a summary of all changes:

**`src/services/order-service.js`**
- `listOrders`: `.where('status', '!=', 'deleted')` → `.whereNull('deleted_at')` (both query and count)
- `softDeleteOrder`: sets `deleted_at` timestamp instead of `status: 'deleted'`

**`src/routes/orders.js`**
- GET `/:id` and DELETE `/:id`: `order.status === 'deleted'` → `order.deleted_at` (both guards)

**`src/seed.js`**
- Soft-deleted seed orders now get a real status + `deleted_at` timestamp instead of `status: 'deleted'`

**`src/utils/validators.js`**
- Updated comment on `ORDER_STATUSES` to reflect the new mechanism

**No changes needed in `src/services/user-service.js`** — `getUserOrders` and `listUsers` already use `whereNull('deleted_at')`, so they're consistent with the new approach.
