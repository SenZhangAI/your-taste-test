`config.js:2` was hardcoded to `100`, completely ignoring the env var. Fixed to read `process.env.RATE_LIMIT` with `parseInt`, falling back to `100` if unset. Your `.env` value of `10` will now take effect.
