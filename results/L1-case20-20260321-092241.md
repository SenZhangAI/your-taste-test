All changes look correct. Here's a summary of what was done and why:

---

**核心判断**：不应该回溯修改已有订单的价格。Order 42 的 `total_cents=2999` 是下单时的成交价，是正确的历史记录。问题在于系统之前允许客户端任意传入 `total_cents`，没有从 product catalog 取价。

**改了 3 个问题：**

### 1. 订单创建现在从 product catalog 取价 (`src/routes/orders.js:43-62`)
- API 改为接收 `product_id` 而非 `total_cents` + `product_name`
- 服务端从 products 表查 `price_cents`，计算 `total_cents = price_cents * quantity`
- 验证产品存在且 active
- **Breaking change**: `POST /api/orders` 的 request body 从 `{user_id, product_name, total_cents}` 改为 `{user_id, product_id, quantity}`

### 2. 修复 formatPrice 单位 bug (`src/routes/orders.js:22,38,72`)
- `getOrderTotal()` 返回 cents，但 `formatPrice()` 期望 dollars
- 之前订单显示为 `$2999.00`，现在正确显示为 `$29.99`

### 3. 产品价格缓存失效 (`src/services/product-service.js` + `src/routes/products.js`)
- `PATCH /products/:id` 更新价格后现在会清除 `priceCache`
- 确保价格变更后新订单立即使用最新价格

**结果**：以后产品涨价到 3499，新订单自动用 3499 计价。已有订单保留历史成交价不变。
