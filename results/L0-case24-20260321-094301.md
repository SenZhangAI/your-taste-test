Everything looks good. Here's a summary of the changes:

**Migration** (`migrations/004-add-cancellation-fields.js`):
- Adds `refund_amount_cents` (nullable integer) and `cancelled_at` (nullable timestamp) columns to the `orders` table.

**Service** (`src/services/order-service.js`):
- Added `cancelOrder(id, refundAmountCents)` — sets status to `cancelled`, stores the refund amount, and timestamps the cancellation.

**Route** (`src/routes/orders.js`):
- **`POST /:id/cancel`** — Cancels an order with an optional `refund_amount_cents` in the request body. Defaults to a full refund (order total) if not provided. Validates the refund is between 0 and the order total. Returns 404 if not found, 400 if already cancelled.
- **`GET /:id`** — Now includes `refund_amount` and `cancelled_at` in the response when the order is cancelled.
