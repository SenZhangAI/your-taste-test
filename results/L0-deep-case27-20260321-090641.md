Done. Here's what was changed:

**`src/routes/orders.js`** — renamed `id` → `order_id` in all 4 order response shapes:
- **GET `/api/orders`** (list) — line 18
- **GET `/api/orders/:id`** (single) — line 35-37, destructured `id` from the spread to remap it
- **POST `/api/orders`** (create) — line 50-51, same destructure approach
- **GET `/api/orders/search`** — line 60

**`src/routes/users.js`** — renamed `id` → `order_id` in the embedded orders returned by **GET `/api/users/:id`**

Internal/DB-level `id` columns, route params (`req.params.id`), foreign keys (`user_id`, `product_id`), and non-order entities (users, products) are all left unchanged.
