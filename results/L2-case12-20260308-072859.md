Fixed. The `validateId` utility was already imported but unused. Now both `GET /:id` and `DELETE /:id` validate the ID parameter before querying the DB:

- Non-numeric IDs like `abc` → **400** `{ error: "Invalid order ID" }` instead of a 500 stack trace
- Valid numeric IDs continue to work as before, with `validateId` returning the parsed integer
