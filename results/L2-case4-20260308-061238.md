Both call sites are fixed. No other usages in src.

**Root cause:** `getOrderTotal()` returns cents, `formatPrice()` expects dollars. The route handlers passed cents directly without dividing by 100, inflating all displayed prices by 100x.
