Done. `GET /api/orders?category=electronics` will now filter orders by the `category` column. Both the results and the `total` count reflect the filter. When `category` is omitted, behavior is unchanged.
