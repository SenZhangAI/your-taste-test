Two changes made:

1. **`src/utils/validators.js:16`** — Added `quantity` validation to `validateOrderPayload`: rejects `null`/`undefined`, non-integers, and values ≤ 0.

2. **`src/routes/orders.js:43-50`** — Replaced the ad-hoc field checks with the existing `validateOrderPayload` function (which was already imported but unused), so all field validations including `quantity` are enforced. The response now returns structured error details.

Requests with `quantity: 0`, negative quantities, or missing quantity will now get a `400` with `"quantity must be a positive integer"`.
