Backend bug. The count query on line 25 was rebuilding from `db('orders')` and only filtering out deleted orders — it never applied the `status` filter. So when a user filtered by `status=pending`, they'd get 3 matching orders but the total would reflect all 47 non-deleted orders.

Fixed by reusing the already-filtered `query` (which includes both the deleted exclusion and the status filter) for the count as well.
