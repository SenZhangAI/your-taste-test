The fix is on `src/services/order-service.js:32` — added `.whereNot('status', 'deleted')` to the query.

**What was wrong:** The `getOrder` function's JSDoc (line 29) said it filters soft-deleted records, but the query had no such filter. This meant `GET /api/orders/:id` would return deleted orders. The route handler at `routes/orders.js:30` did have a secondary check (`order.status === 'deleted'`), but that's defense-in-depth — the service layer should enforce this as the source of truth, especially since `getOrder` is also called from `createOrder` (returning a newly created order) and the DELETE endpoint.

**The fix:** Added `.whereNot('status', 'deleted')` so the DB query itself excludes soft-deleted records, matching the documented contract. The route-level check is now redundant but harmless to keep as a safety net.
