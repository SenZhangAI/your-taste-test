Done. Changes across 4 files:

- **`order-service.js`** — `listOrders` uses `.whereNull('deleted_at')` for both query and count; `softDeleteOrder` sets `deleted_at` timestamp instead of `status: 'deleted'`
- **`routes/orders.js`** — both GET `/:id` and DELETE `/:id` check `order.deleted_at` instead of `order.status === 'deleted'`
- **`seed.js`** — deleted orders now get a real status + `deleted_at` timestamp instead of `status: 'deleted'`
- **`validators.js`** — removed the now-irrelevant comment about `'deleted'` status
