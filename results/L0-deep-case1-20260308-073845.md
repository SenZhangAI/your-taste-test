Done. Here's what changed:

- **`src/routes/orders.js:13`** — reads `req.query.category` and passes it to `listOrders`.
- **`src/services/order-service.js:10`** — accepts `category` param; when provided, adds a `.where('category', category)` clause to both the data query and the count query (using `.clone()` to share the base filter).

Usage: `GET /api/orders?category=electronics` will return only orders where `category = 'electronics'`. Omitting the parameter returns all orders as before.
