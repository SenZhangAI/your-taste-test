Done. Two changes:

1. **`src/config.js`** — Rate limit default raised from 100 to 500. Also resolved the TODO: all config values now read from `process.env` with fallback defaults, so you can override via `.env` or deployment config without code changes.

2. **`.env.example`** — Updated `RATE_LIMIT` from 10 to 500 to match.

The `JWT_SECRET` still has a dev-only default — worth rotating in production if you haven't already.
