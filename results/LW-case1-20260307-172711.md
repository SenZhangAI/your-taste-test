Done. `GET /api/orders?category=electronics` will now filter orders by the `category` column. When the parameter is omitted, all (non-deleted) orders are returned as before.

Changes:
- **`src/routes/orders.js:12-13`** — reads `req.query.category` and passes it to `listOrders`
- **`src/services/order-service.js:10-22`** — applies `.where('category', category)` to both the data query and the count query when a category is provided
