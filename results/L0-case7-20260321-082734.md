Two changes made:

1. **`src/utils/validators.js:16`** — Added `quantity` to `validateOrderPayload`. It now rejects quantities that are `null`, non-integer, or `<= 0` with the error `"quantity must be a positive integer"`.

2. **`src/routes/orders.js:45-48`** — Replaced the ad-hoc field checks with the existing `validateOrderPayload` function (which was imported but never used). This ensures all validation — including the new quantity check — runs on order creation.
