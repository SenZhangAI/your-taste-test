Fixed. The `getOrder` function at `src/services/order-service.js:32` now filters out soft-deleted records with `.whereNot('status', 'deleted')`, matching what the JSDoc promises.

The route-level check at `src/routes/orders.js:30` (`order.status === 'deleted'`) is now redundant but harmless as defense-in-depth — no need to remove it.
