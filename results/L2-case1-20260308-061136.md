Done. `GET /api/orders?category=electronics` now filters by the `category` column. The base query uses `.clone()` so both the results and the count reflect the same filter. When `category` is omitted, all non-deleted orders are returned as before.
