Fixed. The problem was in `getOrderTotal` (`src/services/order-service.js:32`): it returned `total_cents * quantity` (a value in cents), but `formatPrice` expects dollars. Dividing by 100 converts cents to dollars, so a Widget Pro at 2999 cents now correctly displays as `$29.99` instead of `$2999.00`.
