Changes:

1. **`quantity`** — now required, must be a positive integer (rejects `0`, negatives, floats, strings, `null`/`undefined`)
2. **`total_cents`** — same validation, and moved out of the generic `!total_cents` check (which would false-positive on `0` but miss strings like `"abc"`)
3. **Separate error messages** per field so the caller knows exactly what's wrong
