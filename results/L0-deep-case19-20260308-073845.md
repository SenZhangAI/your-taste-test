Here's a summary of the three fixes:

1. **`src/services/order-service.js:56`** — `softDeleteOrder` now sets `deleted_at: db.fn.now()` alongside the existing `status: 'deleted'` update.

2. **`src/services/product-service.js:15`** — `listProducts` was filtering on `is_active` which doesn't exist in the schema. Changed to `where('status', 'active')` to match the actual `status` column (default `'active'`).

3. **`src/routes/products.js:22`** — The single-product route was checking `!product.is_active` (always undefined). Changed to `product.status !== 'active'` so it correctly 404s on inactive products.

Both tables now use the same `status` column pattern for soft-delete filtering, and orders properly record the `deleted_at` timestamp.
