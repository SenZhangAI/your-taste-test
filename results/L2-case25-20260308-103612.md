The fix reuses the same `query` (which already has the status filter applied) for the count, instead of building a separate unfiltered count query.

**Before:** count query = `WHERE status != 'deleted'` (always)  
**After:** count query = `WHERE status != 'deleted' AND status = ?` (when filtered)

Not a frontend bug — the API was returning the wrong `total` in the response payload.
