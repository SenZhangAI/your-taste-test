Here's what I changed:

1. **`src/services/order-service.js`** — Added a `LEFT JOIN` on the `users` table in `listOrders()`, selecting `users.name as user_name` alongside all order columns. This fetches user names in a single query rather than doing N+1 lookups. Also qualified the `status` column in the count query to avoid ambiguity.

2. **`src/routes/orders.js`** — Added `user_name` to the formatted order response object, defaulting to `null` for orders where the user no longer exists (handled by the LEFT JOIN).
