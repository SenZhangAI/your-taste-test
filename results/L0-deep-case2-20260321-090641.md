Here's a summary of all changes:

**`src/services/order-service.js`:**
- `listOrders` — both the query and count query now use `.whereNull('deleted_at')` instead of `.where('status', '!=', 'deleted')`
- `searchOrders` — same swap
- `getOrder` — added `.whereNull('deleted_at')` so deleted orders are excluded at the service level
- `softDeleteOrder` — now sets `deleted_at: db.fn.now()` instead of `status: 'deleted'`

**`src/routes/orders.js`:**
- GET `/:id` and DELETE `/:id` — removed the redundant `order.status === 'deleted'` checks (since `getOrder` now filters them out)
- GET `/stats` — switched the raw query to `.whereNull('deleted_at')`

This aligns orders with the same timestamp-based soft-delete pattern your user service already uses.
