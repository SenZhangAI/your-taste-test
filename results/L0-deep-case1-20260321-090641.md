Done. Here's what I changed:

1. **`src/db.js:14`** — Added `category` column (nullable, defaults to `'general'`) to the `orders` table schema in `initDB()`, matching the existing migration definition.

2. **`src/services/order-service.js:10`** — Added `category` to `listOrders` params. When provided, it filters with `.where('category', category)`, following the same pattern as the existing `status` filter.

3. **`src/routes/orders.js:15`** — Extracts `req.query.category` and passes it to `listOrders`.

Usage: `GET /api/orders?category=electronics` will return only orders with `category = 'electronics'`.
