Fixed both `GET /:id` and `DELETE /:id` routes. The `validateId` utility was already imported but never used — now both routes validate the ID param and return a `400` with `{ error: 'Invalid order ID' }` for non-numeric IDs like `abc`, instead of letting the bad value hit the database and causing a 500.
