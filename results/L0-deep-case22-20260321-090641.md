The LIKE query is fine. The issue was purely that Express was treating `"search"` as an `:id` parameter. Moving `/search` before `/:id` ensures the literal path matches first. Same thing applies to the `/stats` route — you may want to move that above `/:id` too for the same reason.
