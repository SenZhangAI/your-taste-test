All three call sites in `src/routes/orders.js` (list, get single, search) are now fixed. The issue was that `getOrderTotal` returns cents and `formatPrice` expects dollars — each function is correct in isolation, but the caller wasn't converting between the two units. The products route already did this correctly with `/ 100`.
