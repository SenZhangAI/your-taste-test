The other route files (`products.js`, `users.js`) don't have async handlers, so they're not affected.

**Summary:** Wrapped all 4 async route handlers in `orders.js` with try/catch blocks. Database errors now return a clean `{ "error": "Failed to ..." }` JSON response with a 500 status, and the full error is logged server-side via `console.error` for debugging.
