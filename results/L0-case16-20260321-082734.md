Here's what I changed:

**`src/routes/orders.js`** — Imported the existing `asyncHandler` utility and wrapped all 6 route handlers with it. This catches any rejected promises and forwards them to Express error middleware instead of crashing or leaking stack traces.

**`src/index.js`** — Added an Express error-handling middleware (4-arg `(err, req, res, next)`) registered after all routes. It logs the error server-side via `console.error` but only returns a clean `{ "error": "Internal server error" }` JSON response with a 500 status to the client.
