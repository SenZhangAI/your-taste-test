Everything looks good. Here's what was added:

**`PATCH /api/orders/:id`** — updates an order before it ships. The changes:

- **`src/services/order-service.js:71-77`** — new `updateOrder(id, fields)` function that applies the updates and sets `updated_at`
- **`src/routes/orders.js:52-98`** — new PATCH route with these guards:
  - Validates the `:id` param is a positive integer
  - Returns **404** if the order doesn't exist (or is soft-deleted)
  - Returns **409** if the order has already shipped/delivered/cancelled — only `pending` and `confirmed` orders can be modified
  - Whitelists updatable fields: `product_name`, `product_id`, `quantity`, `total_cents`, `status`
  - Returns **400** if no valid fields provided, status is invalid, or quantity/total_cents aren't positive integers
  - Returns the updated order formatted consistently with the GET endpoint
