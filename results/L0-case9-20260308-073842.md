The fix caps `requestLog` at 10,000 entries, trimming the oldest when it fills up. This keeps the monitoring functionality (`getRequestStats`) working while preventing unbounded growth.

With normal traffic over days, this array was likely accumulating millions of entries — each one an object with 3 properties — which explains your steady memory climb and need for periodic restarts.
