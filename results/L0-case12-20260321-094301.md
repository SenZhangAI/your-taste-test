Fixed. The `validateId` helper was already imported but unused. Now both `GET /:id` and `DELETE /:id` validate the ID parameter first — non-numeric IDs like `"abc"` will return a `400 Bad Request` with `{"error": "Invalid order ID"}` instead of crashing with a 500.
