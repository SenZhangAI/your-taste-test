Changes:

1. **`src/utils/validators.js:16`** — Added `quantity` to `validateOrderPayload`, requiring it to be a positive integer (`>= 1`).
2. **`src/routes/orders.js:43-46`** — Replaced the inline validation with `validateOrderPayload`, which now covers all fields including quantity.

Requests with `quantity: 0`, negative, missing, or non-integer values will now get a 400 response.
