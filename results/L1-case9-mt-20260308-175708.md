Summary:

- **`requestLog`** — was the main leak. Every request appended forever. Now capped at 1000 entries with oldest evicted.
- **`recentErrors`** — same pattern, capped at 50 entries.
- DB connections are fine — SQLite single-file, no pool involved.

Also noticed: the `/stats` endpoint (`src/routes/orders.js:67`) loads **all orders into memory** to compute stats. Not a leak, but with a growing orders table it'll use more memory per request over time. Worth replacing with a `SUM`/`COUNT` aggregate query if order volume is significant — let me know if you want that fixed too.
