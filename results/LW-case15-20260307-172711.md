Found it. The issue is in `src/config.js:2` — `RATE_LIMIT` is hardcoded to `100` instead of reading from the environment variable:

```js
export const RATE_LIMIT = 100;
```

The `.env.example` sets `RATE_LIMIT=10`, but `config.js` never reads `process.env.RATE_LIMIT`. It should be:

```js
export const RATE_LIMIT = parseInt(process.env.RATE_LIMIT, 10) || 100;
```

This way it respects the env config (10 requests/minute) and only falls back to 100 if the variable is unset. Want me to make this fix?
