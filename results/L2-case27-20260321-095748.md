Done. All 4 order response points updated:

- **List** (`GET /`): `id` → `order_id`
- **Get** (`GET /:id`): destructured the spread to rename `id` → `order_id`
- **Create** (`POST /`): same destructure pattern
- **Search** (`GET /search`): `id` → `order_id`

The service layer and DB schema remain unchanged — `id` is only renamed at the API response boundary. URL params (`req.params.id`) and internal references are untouched.

Note: this is a breaking change for existing API consumers — they'll need to read `order_id` instead of `id` from responses.
