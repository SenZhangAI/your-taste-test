Done. Here's what changed across 5 response points:

**`src/routes/orders.js`:**
- `GET /` (list) — `id: o.id` → `order_id: o.id`
- `GET /:id` (detail) — destructure `id` from spread, output as `order_id`
- `POST /` (create) — destructure `id` from returned row, output as `order_id`
- `GET /search` — `id: o.id` → `order_id: o.id`

**`src/routes/users.js`:**
- `GET /:id` (user detail with orders) — map nested orders to rename `id` → `order_id`

The DB column and internal service layer are unchanged — only the API response serialization was updated.
