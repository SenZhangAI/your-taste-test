Done. The orders list endpoint now includes `user_name` for each order by looking up the user via `getUser()` (which caches results in memory).

**Breadth-scan:** The `/search` endpoint (line 53) also returns orders without user info — same gap. The single order `GET /:id` endpoint returns the raw order which already includes `user_id` but no name. Want me to add `user_name` to those endpoints too?
