Changes made:

1. **`src/utils/validators.js:16`** — Added `quantity` to `validateOrderPayload`: rejects `null`, non-integers, and values < 1.
2. **`src/routes/orders.js:45-47`** — Replaced the inline validation with the existing `validateOrderPayload` (which was imported but unused). Now returns all validation errors, not just a generic "missing fields" message.

This means `quantity: 0`, `quantity: -1`, `quantity: 1.5`, and missing quantity all get rejected with a clear error message.
