**That's the actual leak.** The customer is seeing their deleted order through `GET /users/:id` (line 26), which calls `getUserOrders`. That function was filtering on `whereNull('deleted_at')`, but `softDeleteOrder` only sets `status = 'deleted'` — never touches `deleted_at`. So deleted orders pass right through.

**Recap of all fixes applied:**

| File | Bug | Fix |
|---|---|---|
| `order-service.js:48` | `getOrder` has no deleted filter (JSDoc lies) | Added `.where('status', '!=', 'deleted')` |
| `user-service.js:35` | `getUserOrders` filters on `deleted_at` which is never set by soft delete | Changed to `.where('status', '!=', 'deleted')` |
| `orders.js:32,82` | Redundant `order.status === 'deleted'` checks | Removed — now handled by service layer |

The customer's order #42 was leaking through the **user detail endpoint** (`GET /users/:id`), not the order endpoints directly. The `getUserOrders` filter was checking the wrong column.
