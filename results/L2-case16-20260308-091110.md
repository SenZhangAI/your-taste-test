Done. Here's what changed:

**`src/routes/orders.js`** — All 6 route handlers wrapped with the existing `asyncHandler` utility. This catches any rejected promise and forwards it to Express error middleware via `next(err)`.

**`src/index.js`** — Added a JSON error handler middleware (registered after all routes) that logs the error and returns `{ error: 'Internal server error' }` with the appropriate status code, instead of leaking stack traces.
