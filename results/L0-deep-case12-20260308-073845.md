The fix uses the already-imported `validateId` helper (which was imported but never called) to validate the `:id` param in both the `GET /:id` and `DELETE /:id` routes. Now `GET /api/orders/abc` will return a `400 Bad Request` with `{"error": "Invalid order ID"}` instead of a 500 with a stack trace.
