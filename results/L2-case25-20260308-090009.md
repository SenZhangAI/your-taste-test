The count query was built from a fresh `db('orders')` instead of the filtered `query`. Now it clones the same `query` (which includes the status filter when present) to get an accurate count.

- **No filter** → counts all non-deleted orders (same as before)
- **Status filter** → counts only orders matching that status (now correct)
