Done. Added:

- **`getAllOrders()`** in `src/services/order-service.js:28` — fetches all non-deleted orders
- **`GET /export`** in `src/routes/orders.js:27` — returns CSV with columns: id, product, quantity, total, status, date

The route is placed before `/:id` so Express matches it correctly. It uses the same `formatPrice`/`formatDate`/`getOrderTotal` helpers as the list endpoint for consistency.
