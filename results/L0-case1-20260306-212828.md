Done. Here's what changed:

- **`src/routes/orders.js:12`** — Reads `req.query.category` and passes it to `listOrders`.
- **`src/services/order-service.js:8-17`** — `listOrders` accepts an optional `category` param. When provided, it adds a `.where('category', category)` clause to both the data query and the count query (using `.clone()` to avoid mutating the shared query builder).

Usage: `GET /api/orders?category=electronics` (combinable with `page` and `limit`). If `category` is omitted, all non-deleted orders are returned as before.
