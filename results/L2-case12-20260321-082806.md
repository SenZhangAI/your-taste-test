Both `GET /:id` and `DELETE /:id` now validate the ID param using the already-imported `validateId`. Non-numeric IDs like `"abc"` will return `400 { error: "Invalid order ID" }` instead of a 500 with a stack trace.
