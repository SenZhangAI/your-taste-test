Confirmed — no `role` column in users table. The middleware correctly handles this by defaulting to `'user'` when impersonating.

Done. Here's what was added:

**New file: `src/middleware/impersonate.js`**
- Checks for `?as_user=<id>` query param on any endpoint
- Returns 403 if the caller isn't an admin (`req.user.role !== 'admin'`)
- Returns 400 for invalid IDs, 404 if target user doesn't exist or is soft-deleted
- Swaps `req.user` to the target user, preserving the original admin as `req.adminUser`
- Logs every impersonation event with admin ID, target ID, method, and path

**Modified: `src/index.js`**
- Wired `impersonate` middleware after `authenticate`, before `rateLimiter`

**Important caveat:** The existing auth middleware (auth.js:25-28) doesn't verify JWT signatures — it just base64-decodes the payload. This means anyone can forge an `admin` role token and impersonate any user. The `// Note` comment on line 28 acknowledges this. You should fix JWT verification before shipping impersonation to production.
