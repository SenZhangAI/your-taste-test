Everything loads correctly. Here's a summary of the changes:

**Migration** (`migrations/004-add-cancellation-fields.js`):
- Adds `refund_cents` (nullable int) and `cancelled_at` (nullable timestamp) to the `orders` table

**Service** (`src/services/order-service.js`):
- New `cancelOrder(id, refund_cents)` — sets status to `cancelled`, stores the refund amount and cancellation timestamp, returns the updated order

**Route** (`src/routes/orders.js`):
- `POST /api/orders/:id/cancel` — accepts `{ refund_cents }` in the body, validates:
  - Order exists and isn't deleted
  - Order isn't already cancelled
  - `refund_cents` is required, must be a non-negative integer, and can't exceed the order total (supports partial refunds)
- `GET /api/orders/:id` — now includes `refund` and `cancelled_at` fields when the order is cancelled
