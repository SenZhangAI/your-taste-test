**Root cause:** Express route ordering. `/:id` is a greedy parameter route — it matches any path segment, including `search` and `stats`. Since it was registered first, `/api/orders/search` was treated as "get order with id=search".

**Fix:** Moved both `/search` and `/stats` above `/:id` so literal paths match before the parameterized catch-all. The LIKE query itself was correct.
