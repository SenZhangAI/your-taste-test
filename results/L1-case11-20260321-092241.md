Dependencies aren't installed (expected in this setup). The code is syntactically valid — the import error is just missing `node_modules`.

Here's what changed:

**`src/services/order-service.js`** — `createOrder` now:
1. Looks up the product by `product_name` (active products only)
2. Returns 400 if product not found
3. Returns 409 if `product.stock < quantity` (defaults quantity to 1)
4. Decrements stock and inserts the order **in a single transaction** — prevents race conditions where two concurrent orders could oversell
5. Auto-sets `product_id` from the looked-up product

**`src/routes/orders.js`** — Route handler catches errors with a `status` property and returns the appropriate HTTP status + message. Unknown errors re-throw to the global error handler.

**Breadth-scan:** No other code path creates orders. The `softDeleteOrder` function doesn't restore stock — if you want stock returned on cancellation, that's a separate concern worth considering.
