Also note the `/stats` endpoint on line 70 has the same issue — it computes revenue in cents but doesn't label it as such. That's less critical since it's an admin-internal value, but worth being aware of.

**Summary:** Both `formatPrice` and `getOrderTotal` are correct individually (as you suspected), but the call sites were passing cents directly to a function expecting dollars — prices were displayed 100x too high. Fixed all 3 customer-facing call sites by dividing by 100.
