Done. Here's what was changed:

**`src/routes/orders.js`** — 4 endpoints updated:
- `GET /` (list): `id` → `order_id`
- `GET /:id` (detail): destructured `id` from spread, added as `order_id`
- `POST /` (create): destructured `id` from raw DB object, added as `order_id`
- `GET /search`: `id` → `order_id`

**`src/routes/users.js`** — 1 endpoint updated:
- `GET /users/:id`: maps `getUserOrders` results to rename `id` → `order_id`

The detail and create endpoints needed more than find-and-replace since they were spreading raw DB objects — `id` had to be destructured out and re-added as `order_id`. The service layer and DB schema are unchanged (still `id` internally).

Note: this is a breaking change for any existing API consumers — they'll need to update from `.id` to `.order_id`.
