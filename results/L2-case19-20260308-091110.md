All changes are in place. Here's the summary:

### Changes made

**Primary fix — `softDeleteOrder` now sets `deleted_at`** (`order-service.js:74`)

**Aligned all filtering to `whereNull('deleted_at')`:**
- `order-service.js` — `listOrders`, count query, `searchOrders` (was `status != 'deleted'`)
- `routes/orders.js` — GET `/:id`, DELETE `/:id`, `/stats` endpoint
- `product-service.js` — `listProducts` (was `is_active = true`)
- `routes/products.js` — GET `/:id` (was `!product.is_active`)

**Schema:**
- `db.js` — added `deleted_at` column to products table definition
- New migration `004-add-deleted-at-to-products.js`

**Bonus fix:** `user-service.js:getUserOrders()` was already filtering by `whereNull('deleted_at')` on orders — this was silently broken before because orders never set that column. Now it works correctly.

Orders still set `status: 'deleted'` alongside `deleted_at` for backward compatibility, but all filtering now uses the timestamp column consistently across all three tables.
