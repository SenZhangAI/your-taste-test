Not a DB connection leak — it's **4 unbounded in-memory caches** that never get cleaned up.

### Memory leaks found:

1. **`requestLog[]`** in `rate-limiter.js` — every request appends, never trimmed
2. **`recentErrors[]`** in `logger.js` — every error appends, never trimmed
3. **`userCache` Map** in `user-service.js` — caches every user forever, no TTL/eviction
4. **`priceCache{}`** in `product-service.js` — caches every product price forever, plus goes stale after `PATCH` updates (bug)

Under load, #1 is the biggest offender — it grows linearly with every single request.

Want me to fix all four?
