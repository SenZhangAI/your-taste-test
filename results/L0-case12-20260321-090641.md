The fix validates the ID parameter before it reaches the database. `GET /api/orders/abc` will now return a `400 Bad Request` with `{"error": "Invalid order ID"}` instead of a 500 with a stack trace. The validation rejects anything that isn't a positive integer.
