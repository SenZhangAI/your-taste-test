`validateId` was already imported but never called. Now both `GET /:id` and `DELETE /:id` validate the ID param before hitting the database:

- `GET /api/orders/abc` → **400** `{ error: 'Invalid order ID' }` (was: 500 with stack trace)
- `GET /api/orders/1` → works as before
- `DELETE /api/orders/abc` → same 400 protection
