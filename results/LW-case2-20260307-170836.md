All done. Here's a summary of the changes:

**`src/services/order-service.js`:**
- `listOrders` queries (lines 15, 21): `.where('status', '!=', 'deleted')` → `.whereNull('deleted_at')`
- `softDeleteOrder` (line 57): sets `deleted_at: db.fn.now()` instead of `status: 'deleted'`

**`src/routes/orders.js`:**
- GET `/:id` (line 30): `order.status === 'deleted'` → `order.deleted_at`
- DELETE `/:id` (line 53): `order.status === 'deleted'` → `order.deleted_at`
