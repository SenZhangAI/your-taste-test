Two changes made:

1. **`src/utils/validators.js:16`** — `validateOrderPayload` now checks `quantity`: it must be a non-null positive integer. Values like `0`, `-1`, `null`, `undefined`, or `1.5` will be rejected.

2. **`src/routes/orders.js:43-46`** — The route now uses `validateOrderPayload` (which was already imported but unused) instead of the inline `if` check, so all validation including quantity is applied consistently.
