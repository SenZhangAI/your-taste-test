Here's what I changed:

**`src/services/order-service.js:71-76`** — `softDeleteOrder` now sets `deleted_at: db.fn.now()` alongside the existing `status: 'deleted'` update.

**`src/services/product-service.js:19-23`** — Added `softDeleteProduct(id)` that sets `is_active: false`, mirroring the orders pattern.

**`src/routes/products.js`** — Imported `softDeleteProduct` and added a `DELETE /:id` endpoint with the same guard pattern as orders (check exists + not already deleted before soft-deleting).
