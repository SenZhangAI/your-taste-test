Now when a product price is updated via `PATCH /products/:id`, all non-deleted orders linked to that product will have their `total_cents` updated to match the new price.

For order 42 specifically — once the product's price was updated to 3499, a PATCH to that product would have synced it. If you need to fix the existing mismatch right now, you can hit `PATCH /products/:id` with `{ "price_cents": 3499 }` for Widget Pro's product ID, and it will update all linked orders including order 42.

**Note:** the `total_cents` on an order represents the per-unit price (it gets multiplied by `quantity` in `getOrderTotal`), so setting it directly to `price_cents` is correct here.
