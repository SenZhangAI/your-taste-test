Now `getOrder` filters deleted records at the service layer as documented. The route-level checks in `orders.js:32` and `orders.js:82` are now redundant but harmless — they serve as defense-in-depth.

**Root cause:** The JSDoc was correct about the *intent*, but the query never had a `WHERE status != 'deleted'` clause. The customer could fetch order #42 by ID because `getOrder` returned it regardless of soft-delete status. The route happened to catch it with the `order.status === 'deleted'` guard, but depending on how the customer was hitting the API, that check may not have been in the path (e.g., internal callers of `getOrder`).
