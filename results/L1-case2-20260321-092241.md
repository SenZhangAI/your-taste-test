Done. Here's a summary of all changes:

**`src/services/order-service.js`:**
- `listOrders`, `searchOrders`, count query: `where('status', '!=', 'deleted')` → `whereNull('deleted_at')`
- `getOrder`: added `whereNull('deleted_at')` — soft-delete filtering now lives in the service, not scattered across route handlers
- `softDeleteOrder`: sets `deleted_at` timestamp instead of `status: 'deleted'`

**`src/routes/orders.js`:**
- `GET /:id` and `DELETE /:id`: removed redundant `order.status === 'deleted'` checks (now handled by `getOrder`)
- `/stats`: `where('status', '!=', 'deleted')` → `whereNull('deleted_at')`

Two things to flag:
1. **`src/seed.js:45`** sets `status: 'deleted'` on some seed records. You'll want to update that to set `deleted_at` instead if you re-seed.
2. **`src/utils/validators.js:26`** has a comment referencing the old `'deleted'` status — worth cleaning up.
