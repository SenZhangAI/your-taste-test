Two changes made:

1. **`src/utils/validators.js:16`** — Added `quantity` to `validateOrderPayload`. It now rejects values that aren't positive integers (catches `0`, negative numbers, `null`, `undefined`, floats).

2. **`src/routes/orders.js:43-46`** — Replaced the inline field checks with a call to `validateOrderPayload`, which already existed but wasn't being used. This ensures all validation (including the new quantity check) runs on order creation.
