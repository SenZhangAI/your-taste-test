Here's what I changed:

**`src/config.js`** — The TODO asked to move values to env vars, so all four config values now read from `process.env` with fallback defaults. `RATE_LIMIT` defaults to **500** (up from 100).

**`.env.example`** — Updated the example rate limit from 10 to **500** to match the new default.

This means you can now override the rate limit (and `PORT`, `JWT_SECRET`, `PAGE_SIZE`) per environment via env vars without code changes — which is what you'd want for different dev/staging/production settings.
