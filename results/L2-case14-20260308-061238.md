Done. POST `/api/orders` now formats the response the same way as `GET /:id` — with `total` and `date` formatted via `formatPrice`/`formatDate`.

One thing to flag: the raw `total_cents` and `created_at` fields will still be present in the spread (`...order`), alongside the formatted `total` and `date`. That's the same behavior as the GET /:id handler, so it's consistent — but worth knowing that clients see both.
