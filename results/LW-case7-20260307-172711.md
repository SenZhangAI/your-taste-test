Here's what I changed:

1. **`src/utils/validators.js:16`** — Added `quantity` to `validateOrderPayload`. It now rejects quantities that aren't positive integers (catches `0`, negative numbers, `undefined`, floats, etc.).

2. **`src/routes/orders.js:43-46`** — Replaced the inline field checks with a call to `validateOrderPayload`, which was already imported but unused. This ensures all validation logic lives in one place.
