Done. Renamed `id` to `order_id` in all 4 order API response locations in `src/routes/orders.js`:

1. **GET `/`** (list) — `id: o.id` → `order_id: o.id`
2. **GET `/:id`** (single) — destructured `id` from the spread object, re-added as `order_id`
3. **POST `/`** (create) — destructured `id` from the returned order, re-added as `order_id`
4. **GET `/search`** — `id: o.id` → `order_id: o.id`

The route parameters (`req.params.id`) and internal DB queries are unchanged since those are internal, not part of the API response contract.
