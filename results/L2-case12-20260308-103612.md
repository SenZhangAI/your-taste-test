Fixed. The root cause was that `validateId` was imported but never called — non-numeric IDs like `"abc"` went straight to the DB and threw.

Changes:
- **`orders.js`**: Added `validateId` check in `GET /:id` and `DELETE /:id` — returns 400 for bad IDs instead of 500
- **`users.js`**: Same fix for `GET /:id` (breadth-scan caught the same gap)

Now `GET /api/orders/abc` → `400 { error: "Invalid order ID" }`.
