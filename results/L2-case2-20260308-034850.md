Done. Here's what changed:

- **`src/services/order-service.js`** — `listOrders` queries now use `.whereNull('deleted_at')` instead of `.where('status', '!=', 'deleted')`. `softDeleteOrder` sets `deleted_at` instead of `status: 'deleted'`.
- **`src/routes/orders.js`** — GET and DELETE route guards check `order.deleted_at` instead of `order.status === 'deleted'`.
- **`src/seed.js`** — Soft-deleted seed rows now get a real status (`pending`/`confirmed`/`shipped`) plus a `deleted_at` timestamp, instead of `status: 'deleted'`.
- **`src/utils/validators.js`** — Updated the comment on `ORDER_STATUSES` to reflect the new approach.

This aligns orders with the same pattern already used in `user-service.js`.
