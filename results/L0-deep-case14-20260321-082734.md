The POST response now applies `formatPrice(getOrderTotal(order))` and `formatDate(order.created_at)` — the same formatting as the GET `/:id` handler. So instead of returning raw fields like `total_cents: 2999`, it will return `total: "$29.99"` and `date: "2026-03-21"`.
