Fixed. The `validateId` utility was already imported in orders but never used. Now all three route files validate `:id` params before hitting the database:

- **`GET /api/orders/:id`** and **`DELETE /api/orders/:id`** — added `validateId`, returns 400 for non-numeric IDs
- **`GET /api/users/:id`** and **`GET /api/products/:id`** — same fix applied (same vulnerability)

`GET /api/orders/abc` will now return `{ "error": "Invalid order ID" }` with a 400 status instead of a 500 stack trace.
