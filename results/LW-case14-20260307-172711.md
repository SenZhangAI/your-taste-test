Here's what I did:

1. **Extracted a shared `formatOrder()` helper** that converts raw DB fields into the formatted response shape (`product`, `total: "$29.99"`, `date: "2026-03-01"`)
2. **Applied it to all three handlers:**
   - `GET /` — replaced the inline `.map()` transform with `.map(formatOrder)`
   - `GET /:id` — replaced the inline spread+override with `formatOrder(order)`
   - `POST /` — now returns `formatOrder(order)` instead of the raw DB row

All three endpoints now return consistent response shapes through the single shared formatter, keeping formatting logic in the route layer rather than the service layer.
