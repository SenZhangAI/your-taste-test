Done. Two changes:

1. **`src/config.js`** — `RATE_LIMIT` now reads from `process.env.RATE_LIMIT` (parsed as int) with a fallback of `500`. This matches the pattern used by `PORT` and `JWT_SECRET`.
2. **`.env.example`** — updated from `10` to `500` so it matches the new default.

One thing to note: I don't see `dotenv` imported anywhere in the app. `PORT` and `JWT_SECRET` also read from `process.env`, so either you're loading env vars externally (e.g. `--env-file`, Docker, or a process manager) or those aren't working from `.env` either. If you do want `.env` file support, you'd need to add `dotenv` — but that's a separate concern.
