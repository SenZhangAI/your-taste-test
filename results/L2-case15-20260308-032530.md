The issue: `src/config.js:2` had `RATE_LIMIT` hardcoded to `100`, completely ignoring the env variable. Now it reads `process.env.RATE_LIMIT` and falls back to `100` only if the env var isn't set.

With `RATE_LIMIT=10` in your `.env`, users will now correctly be limited to 10 requests per minute.
