Done. Here's what changed and why:

- **`config.js`**: `RATE_LIMIT` now reads from `process.env.RATE_LIMIT` with a default of 500. Previously it was hardcoded at 100, ignoring any env config — unlike `PORT` and `JWT_SECRET` which already read from env.
- **`.env.example`**: Updated from 10 to 500 to match the new default.

This means deployments can now actually tune the rate limit via `.env` without code changes, which is what `docs/API.md` already claimed was possible.
