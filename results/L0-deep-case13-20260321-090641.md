This isn't actually a string-vs-numeric sorting bug — **price sorting was never implemented**. Here's what's happening:

- `order-service.js:4` defines `SORTABLE_FIELDS = ['created_at', 'updated_at']` — `price` is not included
- `order-service.js:12` falls back to `created_at` when an unsupported sort field is passed
- So `?sort=price` silently sorts by `created_at`, which makes the order appear wrong

The API docs (`docs/API.md`) incorrectly list `price` as a supported sort value, but the feature was never built.

I can add price sorting properly using the `total_cents` integer column (which avoids any string comparison issues). Want me to go ahead?
