Two changes:

1. **`src/utils/validators.js:16`** — Added `quantity` validation: must be a positive integer (`>= 1`). This rejects `0`, negative values, floats, and non-numbers.

2. **`src/routes/orders.js:43-46`** — Replaced the manual field check with the existing `validateOrderPayload` (was imported but unused). Now all validation lives in one place.
