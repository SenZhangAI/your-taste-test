Now that `getOrder()` properly filters at the service layer, the redundant check in the route can stay (defense in depth) but the route handler for `GET /:id` and `DELETE /:id` should handle the `null` case correctly, which they already do (`if (!order || order.status === 'deleted')`). The `order.status === 'deleted'` check is now technically unreachable but harmless.

**Summary:**

- **Not a caching issue.** The JSDoc on `getOrder()` was wrong — it claimed to filter deleted records but didn't.
- **Root cause:** `getUserOrders()` filtered on `deleted_at` (always null) instead of `status != 'deleted'`. The customer's order endpoint used a different soft-delete convention than the rest of the codebase.
- **Fixed both:** `getOrder()` now filters at the service layer, and `getUserOrders()` uses the correct `status` check.
