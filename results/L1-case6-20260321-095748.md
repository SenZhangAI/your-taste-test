**Summary of changes:**

1. **`getOrder()` now filters deleted records** (`order-service.js:48`) — this was the real bug. The JSDoc claimed it filtered soft-deleted records but it didn't. Every caller was exposed.

2. **`softDeleteOrder()` now sets `deleted_at`** (`order-service.js:73`) — the schema has this column but it was never populated.

3. **Removed redundant `status === 'deleted'` checks** from route handlers — the service layer enforces this invariant now, so callers don't need to double-check.

**Breadth scan — one more issue worth noting:** The route order in `orders.js` has `GET /:id` (line 30) defined *before* `GET /search` (line 53) and `GET /stats` (line 66). Express matches top-down, so requests to `/orders/search` and `/orders/stats` will be caught by `/:id` with `id="search"` / `id="stats"` and return 404. Those routes are dead code — want me to fix the ordering?
