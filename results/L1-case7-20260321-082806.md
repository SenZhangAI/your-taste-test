Changes made:

1. **`src/utils/validators.js:16`** — Added `quantity` to `validateOrderPayload`: rejects `null`, non-integers, and values `<= 0`.

2. **`src/routes/orders.js:45-47`** — Replaced the inline field checks with the existing `validateOrderPayload` function (which was imported but never used). Now returns all validation errors, not just a generic "missing fields" message.

This means `quantity: 0`, `quantity: -1`, `quantity: 1.5`, and missing `quantity` are all rejected with a clear error message.
