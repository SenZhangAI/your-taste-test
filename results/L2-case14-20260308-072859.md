Done. Two changes:

1. **POST `/api/orders`** now formats the response with `total` and `date`, matching GET.
2. **Fixed cents→dollars bug** in all three handlers — `getOrderTotal()` returns cents, but `formatPrice()` expects dollars, so added `/ 100`. Without this, a 2999-cent item would display as `$2999.00` instead of `$29.99`.
