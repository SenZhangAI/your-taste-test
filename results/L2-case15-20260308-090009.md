Fixed. The issue was in `src/config.js:2` — `RATE_LIMIT` was hardcoded to `100` instead of reading from `process.env.RATE_LIMIT`. Now it reads the env var and falls back to `100` as default.

With your `.env` set to `RATE_LIMIT=10`, the limiter will now correctly cap at 10 requests per minute per IP. Make sure `dotenv` (or equivalent) is loaded before `config.js` is imported — otherwise `process.env.RATE_LIMIT` will still be undefined.
