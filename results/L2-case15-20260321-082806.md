The bug: `src/config.js:2` had `RATE_LIMIT` hardcoded to `100`, completely ignoring the env var. Same pattern as `PORT` on line 1, but someone forgot to wire it up.

Fixed to `parseInt(process.env.RATE_LIMIT, 10) || 100` — reads the env var with `100` as fallback, matching what `.env.example` documents. Your `RATE_LIMIT=10` will now take effect.
