Done. Four changes made:

- **`order-service.js`**: `listOrders` now filters with `.whereNull('deleted_at')` instead of `.where('status', '!=', 'deleted')`
- **`order-service.js`**: `softDeleteOrder` sets `deleted_at` instead of `status: 'deleted'`
- **`orders.js`**: Both the GET `/:id` and DELETE `/:id` routes check `order.deleted_at` instead of `order.status === 'deleted'`

Note: `listOrders` already excludes soft-deleted rows at the query level, so the route handler doesn't need an additional check there. The `getOrder` function still returns unfiltered results (used by both GET and DELETE to check existence before acting), which is correct — the caller decides how to handle deleted records.
