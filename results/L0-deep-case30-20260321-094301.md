Everything looks good. Here's what was added:

**`POST /api/orders/bulk`** — accepts `{ orders: [...] }` in the request body.

**Route** (`src/routes/orders.js:52-74`):
- Validates the body contains a non-empty `orders` array
- Validates each order has `user_id`, `product_name`, and positive `total_cents`
- Returns `400` with per-index error details on validation failure
- Returns `201` with all created orders on success

**Service** (`src/services/order-service.js:71-87`):
- `createBulkOrders()` inserts all orders inside a **database transaction** — if any insert fails, they all roll back
- Returns the fully hydrated order rows after commit

**Example request:**
```json
POST /api/orders/bulk
{
  "orders": [
    { "user_id": 1, "product_name": "Widget", "quantity": 2, "total_cents": 1999 },
    { "user_id": 1, "product_name": "Gadget", "quantity": 1, "total_cents": 4999 }
  ]
}
```
