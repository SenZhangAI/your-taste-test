Everything looks good. Here's what I changed:

**New file:**
- `migrations/004-add-refund-to-orders.js` — adds `refund_cents` (integer, nullable) and `cancelled_at` (timestamp, nullable) to orders

**Modified files:**

- `src/db.js` — new columns in `createTable` for fresh DBs + auto-migration for existing DBs via `PRAGMA table_info`
- `src/services/order-service.js` — new `cancelOrder(id, refundCents)` function that validates status (only `pending`/`confirmed`), validates refund amount (1 to order total), then sets status to `cancelled` with refund and timestamp
- `src/routes/orders.js` — new `POST /:id/cancel` endpoint accepting `{ refund_cents }`, and GET `/:id` now includes `refund` and `cancelled_at` fields for cancelled orders

**API usage:**

```
POST /api/orders/123/cancel
{ "refund_cents": 1500 }
```

Returns the cancelled order with `refund: "$15.00"`, `cancelled_at: "2026-03-21"`. Partial refunds supported — `refund_cents` can be any value from 1 to the order total (`total_cents * quantity`).
