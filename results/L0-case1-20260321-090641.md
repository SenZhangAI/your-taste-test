Done. Here's what changed:

- **`src/routes/orders.js`**: Extracts `req.query.category` and passes it to `listOrders`.
- **`src/services/order-service.js`**: Accepts `category` param, adds `.where('category', category)` filter when present, and reuses the filtered query for the pagination count (previously the count query ignored filters, so I fixed that too).

Usage: `GET /api/orders?category=electronics` will return only orders with `category = 'electronics'`.
