Done. Summary of changes:

1. **`src/config.js`** — `RATE_LIMIT` now reads from `process.env.RATE_LIMIT` with 500 as the default (was hardcoded to 100, ignoring env vars — unlike `PORT` and `JWT_SECRET` which already read from env)
2. **`.env.example`** — Updated from 10 to 500
3. **`docs/API.md`** — Updated default from 100 to 500

The root issue was that `RATE_LIMIT` was hardcoded, so `.env` settings were silently ignored. Now it's actually configurable, and you can tune it per environment without code changes.
