Done. Here's what I changed:

- **`src/services/order-service.js`**: Added a `LEFT JOIN` on the `users` table in `listOrders()`, selecting `users.name as user_name`. This fetches all user names in a single query (no N+1). The LEFT JOIN ensures orders with missing/deleted users still appear (with `user_name` as null).

- **`src/routes/orders.js`**: Added `user_name` to the formatted order response, defaulting to `null` if the user doesn't exist.
