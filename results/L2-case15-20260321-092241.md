The bug: `src/config.js:2` had `RATE_LIMIT` hardcoded to `100`, completely ignoring the env var. Same pattern as `PORT` on line 1 — but someone forgot the `process.env` read for `RATE_LIMIT`.

Fixed to `parseInt(process.env.RATE_LIMIT, 10) || 100` — reads your `.env` value, falls back to 100 if unset.
