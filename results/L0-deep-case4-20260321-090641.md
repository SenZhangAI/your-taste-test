All three call sites are fixed. Each individually looked correct (as you noticed), but the bug was at the boundary — `getOrderTotal` outputs cents while `formatPrice` consumes dollars, and nothing was converting between the two. Prices were being displayed 100x too high.
