The POST `/api/orders` response now applies the same `formatPrice` and `formatDate` formatting as the GET `/:id` handler, so fields like `total_cents: 2999` will come back as a formatted `total` string and `created_at` as a formatted `date`.
