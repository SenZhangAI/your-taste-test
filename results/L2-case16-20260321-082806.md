Changes made:

1. **`src/routes/orders.js`** — Imported and wrapped all 6 route handlers with the existing `asyncHandler` utility. This catches any thrown/rejected errors and forwards them to Express error middleware via `next(err)`.

2. **`src/index.js`** — Added Express error-handling middleware (4-arg function) after all routes. It logs the error server-side but returns only `{ "error": "Internal server error" }` to the client — no stack traces leaked.
