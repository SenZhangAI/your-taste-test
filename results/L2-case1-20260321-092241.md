Done. Two changes:

1. **`src/routes/orders.js:14`** — reads `?category=` from query params and passes it to the service.
2. **`src/services/order-service.js:10`** — accepts `category`, adds a `.where('category', category)` clause when present. Also fixed the count query to reuse the filtered `query` instead of an unfiltered base query (the old count ignored `status` and would have ignored `category` too, giving wrong pagination totals).

Usage: `GET /api/orders?category=electronics` — returns only orders with `category = 'electronics'`. Combinable with existing params: `?category=electronics&status=pending&page=2`.
